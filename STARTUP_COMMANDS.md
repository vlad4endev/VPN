# Команды для полного запуска проекта

## 📋 Быстрый старт

### 1. Установка зависимостей основного проекта

```bash
# Перейти в корневую директорию проекта
cd /Users/vl4endev/Desktop/VPN

# Установить все зависимости
npm install
```

### 2. Установка зависимостей прокси-сервера (опционально)

```bash
# Перейти в папку server
cd server

# Установить зависимости прокси-сервера
npm install

# Вернуться в корневую директорию
cd ..
```

### 3. Настройка переменных окружения

#### Основной проект (.env в корне)

```bash
# Создать файл .env в корне проекта
touch .env
```

Заполните `.env` следующими переменными:

```env
# Firebase Configuration
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# 3x-ui API credentials (для использования в браузере)
VITE_XUI_USERNAME=your_3xui_username
VITE_XUI_PASSWORD=your_3xui_password
VITE_XUI_INBOUND_ID=your_inbound_id

# VPN Server Configuration
VITE_VLESS_SERVER=your-server-domain.com
VITE_VLESS_PORT=443
```

#### Прокси-сервер (.env в папке server)

```bash
# Создать файл .env в папке server
touch server/.env
```

Заполните `server/.env`:

```env
# 3x-ui Proxy Server Configuration
XUI_HOST=http://your-server-ip:port
XUI_USER=your_3xui_username
XUI_PASS=your_3xui_password
PORT=3001
NODE_ENV=production
```

## 🚀 Запуск проекта

### Режим разработки (Development)

#### Вариант 1: Только фронтенд (без прокси-сервера)

```bash
# В корневой директории
npm run dev
```

Проект будет доступен по адресу: `http://localhost:5173`

#### Вариант 2: С прокси-сервером (рекомендуется для полной функциональности)

**Терминал 1 - Прокси-сервер:**
```bash
cd server
npm run dev
# или для продакшена:
npm start
```

**Терминал 2 - Фронтенд:**
```bash
# В корневой директории
npm run dev
```

### Режим продакшена (Production)

#### 1. Сборка проекта

```bash
# В корневой директории
npm run build
```

Собранные файлы будут в папке `dist/`

#### 2. Предпросмотр продакшен-сборки

```bash
npm run preview
```

#### 3. Запуск прокси-сервера в продакшене

```bash
cd server
npm start
```

Или с PM2 (для постоянной работы):

```bash
cd server

# Установить PM2 глобально (если еще не установлен)
npm install -g pm2

# Запустить прокси-сервер через PM2
npm run pm2:start

# Проверить статус
pm2 status

# Просмотр логов
pm2 logs xui-proxy

# Остановить
npm run pm2:stop

# Перезапустить
npm run pm2:restart
```

## 📝 Полный скрипт установки и запуска

### Для macOS/Linux:

```bash
#!/bin/bash

# Цвета для вывода
GREEN='\033[0;32m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

echo -e "${BLUE}🚀 Установка и запуск SkyPuth VPN${NC}"

# 1. Установка зависимостей основного проекта
echo -e "${GREEN}📦 Установка зависимостей основного проекта...${NC}"
npm install

# 2. Установка зависимостей прокси-сервера
echo -e "${GREEN}📦 Установка зависимостей прокси-сервера...${NC}"
cd server
npm install
cd ..

# 3. Проверка наличия .env файлов
if [ ! -f .env ]; then
    echo -e "${BLUE}⚠️  Файл .env не найден. Создайте его и заполните переменные окружения.${NC}"
    echo "См. инструкции выше или файл SETUP_ENV.md"
fi

if [ ! -f server/.env ]; then
    echo -e "${BLUE}⚠️  Файл server/.env не найден. Создайте его для работы прокси-сервера.${NC}"
fi

# 4. Запуск в режиме разработки
echo -e "${GREEN}✅ Зависимости установлены!${NC}"
echo -e "${BLUE}💡 Для запуска проекта используйте:${NC}"
echo "   npm run dev"
echo ""
echo -e "${BLUE}💡 Для запуска с прокси-сервером откройте два терминала:${NC}"
echo "   Терминал 1: cd server && npm run dev"
echo "   Терминал 2: npm run dev"
```

Сохраните этот скрипт как `setup.sh` и сделайте его исполняемым:

```bash
chmod +x setup.sh
./setup.sh
```

## 🔧 Полезные команды

### Проверка версий

```bash
# Версия Node.js (требуется >= 18.0.0)
node --version

# Версия npm
npm --version

# Проверка установленных зависимостей
npm list --depth=0
```

### Очистка и переустановка

```bash
# Удалить node_modules и package-lock.json
rm -rf node_modules package-lock.json
rm -rf server/node_modules server/package-lock.json

# Переустановить зависимости
npm install
cd server && npm install && cd ..
```

### Анализ бандла

```bash
npm run analyze
```

После выполнения откройте `dist/stats.html` для просмотра анализа.

## 🌐 Порты по умолчанию

- **Frontend (Vite Dev Server)**: `http://localhost:5173`
- **Frontend (Vite Preview)**: `http://localhost:4173`
- **Proxy Server**: `http://localhost:3001` (настраивается в server/.env)

## ⚠️ Важные замечания

1. **Node.js версия**: Требуется Node.js >= 18.0.0
2. **Переменные окружения**: Обязательно настройте `.env` файлы перед запуском
3. **Firebase**: Убедитесь, что Firebase проект настроен и правила Firestore применены
4. **3x-ui**: Прокси-сервер необходим для работы с 3x-ui API из браузера (CORS)

## 🐛 Решение проблем

### Проблема: "Cannot find module"
```bash
# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install
```

### Проблема: "Port already in use"
```bash
# Найти процесс, использующий порт
lsof -ti:5173  # для Vite
lsof -ti:3001  # для прокси-сервера

# Убить процесс
kill -9 $(lsof -ti:5173)
```

### Проблема: "Firebase not initialized"
- Проверьте, что все переменные `VITE_FIREBASE_*` заполнены в `.env`
- Убедитесь, что файл `.env` находится в корне проекта

## 📚 Дополнительная документация

- `README.md` - Основная документация
- `SETUP_ENV.md` - Детальная настройка переменных окружения
- `DEPLOYMENT.md` - Инструкции по деплою
- `QUICK_START.md` - Быстрый старт
