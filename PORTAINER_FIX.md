# 🔧 Решение проблемы: "не удалось найти ссылку «main»"

> Если Portainer не может найти ветку main в репозитории

---

## ⚠️ Проблема

Ошибка: `Не удалось получить идентификатор репозитория git: не удалось найти ссылку «main» в репозитории`

---

## ✅ Решение 1: Web Editor (рекомендуется, если ветка main не доступна)

### Шаг 1: Подготовка файла

1. Откройте файл `portainer-stack.yml` в вашем проекте
2. Скопируйте **все содержимое** файла

### Шаг 2: Создание Stack в Portainer

1. Откройте **Portainer** → **Stacks** → **Add stack**
2. **Name**: `skyputh-vpn`
3. **Build method**: Выберите **Web editor** (не Repository!)
4. Вставьте содержимое `portainer-stack.yml` в редактор

### Шаг 3: Настройка Build Context

⚠️ **ВАЖНО**: Для Web Editor нужно указать путь к коду на сервере.

**Вариант A: Если код уже на сервере**

1. Скопируйте проект на сервер с Portainer:
   ```bash
   # На сервере
   cd /opt
   git clone https://github.com/vlad4endev/VPN.git skyputh-vpn
   cd skyputh-vpn
   ```
2. В Portainer → **Editor** → измените `build.context`:
   ```yaml
   build:
     context: /opt/skyputh-vpn  # путь на сервере
     dockerfile: Dockerfile
   ```

**Вариант B: Используйте готовый образ (если уже собран)**

Измените в редакторе:
```yaml
services:
  skyputh-vpn:
    image: skyputh-vpn:latest  # вместо build
    # ... остальное без изменений
```

### Шаг 4: Добавьте переменные окружения

В разделе **Environment variables** добавьте все переменные (см. ниже)

### Шаг 5: Развертывание

Нажмите **Deploy the stack**

---

## ✅ Решение 2: Push ветки main на GitHub

Если ветка `main` еще не запушена на GitHub:

### Шаг 1: Проверка локальных веток

```bash
cd /Users/vl4endev/Desktop/VPN
git branch --show-current  # должна быть main
```

### Шаг 2: Push ветки на GitHub

```bash
# Убедитесь, что вы в ветке main
git checkout main

# Push ветки main на GitHub
git push -u origin main

# Или если первый раз
git push origin main
```

### Шаг 3: Проверка на GitHub

1. Откройте `https://github.com/vlad4endev/VPN`
2. Убедитесь, что ветка `main` видна
3. Убедитесь, что файл `portainer-stack.yml` есть в репозитории

### Шаг 4: Повторите установку в Portainer

Теперь используйте **Repository** метод с веткой `main`

---

## ✅ Решение 3: Использование конкретного коммита

Если ветка недоступна, используйте конкретный коммит:

1. В Portainer → **Repository URL**: `https://github.com/vlad4endev/VPN.git`
2. **Reference**: Используйте хеш коммита вместо `main`:
   ```
   Reference: 099ee77
   ```
   Или полный хеш:
   ```
   Reference: 099ee77...
   ```

⚠️ **Минус**: При обновлениях нужно будет менять хеш коммита.

---

## ✅ Решение 4: Использование тега (если есть)

Если в репозитории есть теги:

1. Создайте тег (на локальной машине):
   ```bash
   git tag v1.0.0
   git push origin v1.0.0
   ```

2. В Portainer используйте:
   ```
   Reference: v1.0.0
   ```

---

## 📝 Рекомендуемый способ для Web Editor

### Полный пример stack для Web Editor:

```yaml
version: '3.8'

services:
  skyputh-vpn:
    build:
      context: /opt/skyputh-vpn  # ИЛИ путь где код на сервере
      dockerfile: Dockerfile
    container_name: skyputh-vpn
    restart: unless-stopped
    
    ports:
      - "3001:3001"
    
    environment:
      - NODE_ENV=production
      - PROXY_PORT=3001
      # ... добавьте все переменные окружения
    # ... остальное без изменений
```

### Или используйте готовый образ:

```yaml
version: '3.8'

services:
  skyputh-vpn:
    image: skyputh-vpn:latest  # если образ уже собран
    container_name: skyputh-vpn
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      # ... добавьте все переменные
    # ... остальное без изменений
```

---

## 🚀 Быстрый старт (Web Editor)

1. Скопируйте содержимое `portainer-stack.yml`
2. **Portainer** → **Stacks** → **Add stack**
3. **Name**: `skyputh-vpn`
4. **Build method**: **Web editor**
5. Вставьте содержимое файла
6. Измените `build.context` на путь к коду на сервере (или используйте `image`)
7. Добавьте переменные окружения
8. **Deploy the stack**

---

## 📚 Переменные окружения

Добавьте в **Environment variables**:

```
XUI_HOST=http://your-3xui-server:2053
XUI_USERNAME=admin
XUI_PASSWORD=your_password
XUI_INBOUND_ID=1

VITE_FIREBASE_API_KEY=your_key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abc123

ALLOWED_ORIGINS=https://yourdomain.com
FRONTEND_URL=https://yourdomain.com
```

---

**Готово!** Выберите подходящий способ и следуйте инструкциям. 🎉
