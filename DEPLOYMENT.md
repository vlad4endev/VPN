# 🚀 Руководство по развертыванию SkyPuth VPN

## 📋 Содержание
1. [Предварительные требования](#предварительные-требования)
2. [Локальная установка и запуск](#локальная-установка-и-запуск)
3. [Настройка окружения](#настройка-окружения)
4. [Развертывание в продакшене](#развертывание-в-продакшене)
5. [Проверка работоспособности](#проверка-работоспособности)
6. [Устранение неполадок](#устранение-неполадок)

---

## 🔧 Предварительные требования

### Необходимое ПО:
- **Node.js** версии 18.x или выше
- **npm** версии 9.x или выше (или **yarn**, **pnpm**)
- **Git** (для клонирования репозитория)

### Проверка версий:
```bash
node --version  # Должно быть >= 18.0.0
npm --version   # Должно быть >= 9.0.0
```

### Внешние сервисы:
1. **Firebase проект** с настроенным Firestore
2. **3x-ui панель** с доступом к API
3. **Веб-сервер** (для продакшена): Nginx, Apache или аналогичный

---

## 💻 Локальная установка и запуск

### Шаг 1: Клонирование и установка зависимостей

```bash
# Если проект еще не скачан, клонируйте репозиторий
# git clone <repository-url>
# cd VPN

# Установите зависимости
npm install
```

### Шаг 2: Настройка переменных окружения

```bash
# Скопируйте пример файла окружения
cp .env.example .env

# Откройте .env в редакторе и заполните все значения
nano .env  # или используйте любой другой редактор
```

**Важно**: Заполните ВСЕ переменные в файле `.env`. Без них приложение не запустится.

### Шаг 3: Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### Шаг 4: Проверка работы

1. Откройте браузер и перейдите на `http://localhost:5173`
2. Попробуйте зарегистрировать нового пользователя
3. Проверьте, что пользователь создается в Firestore
4. Проверьте, что клиент создается в панели 3x-ui

---

## ⚙️ Настройка окружения

### Firebase настройка

1. Перейдите в [Firebase Console](https://console.firebase.google.com/)
2. Выберите ваш проект (или создайте новый)
3. Перейдите в **Project Settings** > **Your apps** > **Web app**
4. Скопируйте значения конфигурации в `.env`:
   ```env
   VITE_FIREBASE_API_KEY=AIza...
   VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
   VITE_FIREBASE_PROJECT_ID=your-project-id
   VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
   VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
   VITE_FIREBASE_APP_ID=1:123456789:web:abc123
   ```

5. Настройте Firestore:
   - Создайте коллекцию: `/artifacts/skyputh/public/data/users_v4`
   - Настройте правила безопасности (для разработки можно разрешить анонимный доступ)

### 3x-ui настройка

1. Убедитесь, что панель 3x-ui доступна и работает
2. Получите учетные данные администратора
3. Найдите ID инбаунда, в который будут добавляться клиенты
4. Заполните в `.env`:
   ```env
   VITE_XUI_USERNAME=admin
   VITE_XUI_PASSWORD=your_password
   VITE_XUI_INBOUND_ID=1
   XUI_HOST=http://your-server-ip:2053
   ```

### VLESS конфигурация (опционально)

Если вы хотите использовать кастомные параметры для VLESS:

```env
VITE_VLESS_SERVER=nl.skyputh.com
VITE_VLESS_PORT=443
VITE_VLESS_PUBLIC_KEY=your-public-key
VITE_VLESS_SHORT_ID=your-short-id
VITE_VLESS_SNI=www.microsoft.com
VITE_VLESS_FINGERPRINT=chrome
```

---

## 🌐 Развертывание в продакшене

### Вариант 1: Статический хостинг (Vercel, Netlify, GitHub Pages)

#### Сборка проекта:
```bash
npm run build
```

Это создаст папку `dist/` со статическими файлами.

#### Деплой на Vercel:
```bash
# Установите Vercel CLI
npm i -g vercel

# Деплой
vercel
```

**Важно**: Добавьте все переменные окружения в настройках проекта на Vercel.

#### Деплой на Netlify:
1. Подключите репозиторий к Netlify
2. Укажите:
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
3. Добавьте все переменные окружения в настройках

### Вариант 2: Собственный сервер (Nginx)

#### 1. Сборка проекта:
```bash
npm run build
```

#### 2. Копирование файлов на сервер:
```bash
# На вашем сервере
scp -r dist/* user@your-server:/var/www/skyputh-vpn/
```

#### 3. Настройка Nginx:

Создайте файл `/etc/nginx/sites-available/skyputh-vpn`:

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    # Редирект на HTTPS (рекомендуется)
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name your-domain.com;
    
    # SSL сертификаты (используйте Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    
    # Корневая директория
    root /var/www/skyputh-vpn;
    index index.html;
    
    # Проксирование запросов к 3x-ui API
    location /api/xui {
        proxy_pass http://your-3xui-server:2053;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cookie_path / /api/xui;
        
        # Важно для работы с cookies
        proxy_cookie_domain your-3xui-server your-domain.com;
    }
    
    # Статические файлы
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Кэширование статических ресурсов
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

#### 4. Активация конфигурации:
```bash
sudo ln -s /etc/nginx/sites-available/skyputh-vpn /etc/nginx/sites-enabled/
sudo nginx -t  # Проверка конфигурации
sudo systemctl reload nginx
```

#### 5. Настройка SSL (Let's Encrypt):
```bash
sudo certbot --nginx -d your-domain.com
```

### Вариант 3: Docker (опционально)

Создайте `Dockerfile`:

```dockerfile
# Build stage
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

# Production stage
FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

Создайте `nginx.conf`:

```nginx
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    
    location /api/xui {
        proxy_pass http://host.docker.internal:2053;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

Сборка и запуск:
```bash
docker build -t skyputh-vpn .
docker run -d -p 80:80 --env-file .env skyputh-vpn
```

---

## ✅ Проверка работоспособности

### Функциональные тесты:

1. **Регистрация пользователя:**
   - Откройте приложение
   - Зарегистрируйте нового пользователя
   - Проверьте, что пользователь появился в Firestore
   - Проверьте, что клиент создан в 3x-ui

2. **Вход пользователя:**
   - Войдите с созданными учетными данными
   - Проверьте отображение личного кабинета

3. **Отображение статистики:**
   - Проверьте загрузку статистики трафика
   - Проверьте отображение статуса подключения

4. **Админ-панель:**
   - Войдите как администратор
   - Проверьте отображение списка пользователей
   - Попробуйте удалить тестового пользователя

### Проверка консоли браузера:

Откройте DevTools (F12) и проверьте:
- Нет критических ошибок в Console
- Нет ошибок 404/500 в Network
- Все запросы к Firebase и 3x-ui успешны

### Проверка логов сервера:

```bash
# Если используете Nginx
sudo tail -f /var/log/nginx/error.log

# Если используете Docker
docker logs <container-id>
```

---

## 🔍 Устранение неполадок

### Проблема: Приложение не запускается

**Решение:**
1. Проверьте, что все зависимости установлены: `npm install`
2. Проверьте, что файл `.env` существует и заполнен
3. Проверьте консоль на наличие ошибок валидации переменных окружения

### Проблема: Ошибка "Firebase не инициализирован"

**Решение:**
1. Проверьте все переменные Firebase в `.env`
2. Убедитесь, что переменные начинаются с `VITE_`
3. Перезапустите dev сервер после изменения `.env`

### Проблема: Ошибка подключения к 3x-ui

**Решение:**
1. Проверьте, что `XUI_HOST` указан правильно
2. Проверьте доступность панели 3x-ui: `curl http://your-server:2053`
3. Проверьте учетные данные `VITE_XUI_USERNAME` и `VITE_XUI_PASSWORD`
4. В продакшене проверьте настройки прокси в Nginx

### Проблема: CORS ошибки

**Решение:**
1. В dev режиме: проверьте настройки прокси в `vite.config.js`
2. В продакшене: настройте прокси на уровне веб-сервера (см. раздел Nginx)

### Проблема: Пользователи не создаются в 3x-ui

**Решение:**
1. Проверьте `VITE_XUI_INBOUND_ID` - должен быть валидным ID инбаунда
2. Проверьте логи в консоли браузера
3. Проверьте доступность API 3x-ui

### Проблема: Статистика не загружается

**Решение:**
1. Проверьте, что у пользователя есть UUID
2. Проверьте, что клиент существует в 3x-ui
3. Проверьте логи в консоли браузера

---

## 📝 Дополнительные рекомендации

### Безопасность:

1. **Никогда не коммитьте `.env` в Git**
   - Убедитесь, что `.env` в `.gitignore`
   - Используйте `.env.example` как шаблон

2. **Используйте HTTPS в продакшене**
   - Настройте SSL сертификаты (Let's Encrypt)
   - Принудительно перенаправляйте HTTP на HTTPS

3. **Настройте правила безопасности Firestore**
   - Ограничьте доступ к данным пользователей
   - Используйте Firebase Authentication для проверки прав

4. **Ограничьте доступ к панели 3x-ui**
   - Используйте firewall для ограничения доступа
   - Используйте сильные пароли

### Производительность:

1. **Включите кэширование статических файлов**
2. **Используйте CDN для статических ресурсов**
3. **Оптимизируйте изображения и ресурсы**
4. **Включите gzip сжатие в Nginx**

### Мониторинг:

1. **Настройте логирование ошибок**
   - Используйте Sentry или аналогичный сервис
2. **Мониторинг производительности**
   - Используйте Google Analytics или аналогичный сервис
3. **Мониторинг доступности**
   - Настройте uptime monitoring (UptimeRobot, Pingdom)

---

## 📞 Поддержка

Если у вас возникли проблемы, не описанные в этом руководстве:

1. Проверьте логи в консоли браузера
2. Проверьте логи на сервере
3. Проверьте документацию Firebase и 3x-ui
4. Создайте issue в репозитории проекта

---

**Последнее обновление**: 2024

