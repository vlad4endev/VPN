# 🐳 Инструкция по деплою через Docker

> Краткая инструкция для запуска проекта на удаленном сервере

---

## 🚀 Быстрый старт (одной командой)

### 1. Подключитесь к серверу

```bash
ssh user@your-server-ip
```

### 2. Клонируйте и запустите

```bash
# Клонируйте репозиторий
git clone https://github.com/vlad4endev/VPN.git skyputh-vpn
cd skyputh-vpn

# Создайте .env.production файл
cp .env.example .env.production
nano .env.production  # Заполните все переменные

# Запустите одной командой
docker-compose up -d --build
```

### 3. Проверьте работоспособность

```bash
# Статус контейнера
docker-compose ps

# Healthcheck
curl http://localhost:3001/health

# Логи
docker-compose logs -f
```

---

## ✅ Готово!

Приложение доступно:
- **http://your-server-ip:3001**
- **https://yourdomain.com** (если настроен Nginx)

---

## 📋 Что включено

### Файлы:

1. **Dockerfile** — оптимизированный multi-stage build
   - Сборка frontend (React/Vite)
   - Подготовка backend зависимостей
   - Финальный легковесный образ (~200MB)

2. **docker-compose.yml** — готовая конфигурация
   - Автоматическая сборка
   - Healthcheck
   - Restart policy
   - Сетевая изоляция

3. **.dockerignore** — исключения
   - node_modules
   - .env файлы
   - .git
   - Документация и бекапы

---

## 🔄 Обновление

```bash
cd ~/skyputh-vpn
git pull origin main
docker-compose up -d --build
```

---

## 📚 Подробная инструкция

См. `DEPLOY_INSTRUCTIONS.md` для полного руководства с настройкой Nginx, SSL и другими деталями.
