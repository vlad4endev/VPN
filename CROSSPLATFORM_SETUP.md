# 🌍 Кроссплатформенная настройка проекта

Этот документ описывает настройку проекта для работы на всех платформах: **Mac**, **Windows** и **Linux**.

## ✅ Выполненные задачи

### 1. Созданы .env.example файлы

- **Frontend** (`.env.example` в корне проекта):
  - `VITE_FIREBASE_API_KEY`
  - `VITE_FIREBASE_AUTH_DOMAIN`
  - `VITE_FIREBASE_PROJECT_ID`
  - `VITE_FIREBASE_STORAGE_BUCKET`
  - `VITE_FIREBASE_MESSAGING_SENDER_ID`
  - `VITE_FIREBASE_APP_ID`

- **Backend** (`server/.env.example`):
  - `FIREBASE_PROJECT_ID`
  - `FIREBASE_CLIENT_EMAIL`
  - `FIREBASE_PRIVATE_KEY` (в одну строку с `\n`)
  - `PORT=3001`
  - `NODE_ENV=development`

### 2. Нормализация переносов строк

Все `.env` файлы используют формат **UTF-8 с LF** (Unix-стиль), что обеспечивает корректную работу на всех ОС.

### 3. Обновлены скрипты package.json

- `npm run dev` — запускает frontend с явным host `127.0.0.1` и портом `5173`
- `npm start` (в server/) — запускает backend на порту `3001`
- `npm run start:all` — универсальный скрипт для запуска всех служб
- `npm run setup:env` — проверка и настройка .env файлов
- `npm run setup:env:fix` — автоматическое исправление .env файлов

### 4. Кроссплатформенные пути

Все пути в Node.js файлах используют:
- `path.join()` вместо строковой конкатенации
- Относительные пути (`./`, `../`) вместо абсолютных
- `fileURLToPath(import.meta.url)` для получения `__dirname` в ES modules

### 5. Обновлены скрипты запуска

- **Linux/Mac**: `start-all.sh` — обновлен для использования правильного host
- **Windows PowerShell**: `start-all.ps1` — обновлен для кроссплатформенности
- **Windows CMD**: `start-all.bat` — обновлен для кроссплатформенности
- **Универсальный**: `scripts/start-all.js` — работает на всех платформах

### 6. Обновлен backend для поддержки Firebase переменных

Backend теперь поддерживает два способа инициализации Firebase Admin SDK:
1. `FIREBASE_SERVICE_ACCOUNT_KEY` (JSON строка) — приоритет
2. `FIREBASE_CLIENT_EMAIL` + `FIREBASE_PRIVATE_KEY` (отдельные переменные)
3. Application Default Credentials (для production)

## 🚀 Быстрый старт

### Шаг 1: Настройка переменных окружения

```bash
# Автоматическая проверка и создание .env файлов
npm run setup:env

# Или с автоматическим исправлением
npm run setup:env:fix
```

Затем заполните переменные в:
- `.env` (frontend)
- `server/.env` (backend)

### Шаг 2: Установка зависимостей

```bash
# Установка зависимостей frontend
npm install

# Установка зависимостей backend
cd server
npm install
cd ..
```

### Шаг 3: Запуск проекта

#### Вариант 1: Универсальный скрипт (рекомендуется)

```bash
npm run start:all
```

#### Вариант 2: Платформо-специфичные скрипты

**Linux/Mac:**
```bash
./start-all.sh
```

**Windows PowerShell:**
```powershell
.\start-all.ps1
```

**Windows CMD:**
```cmd
start-all.bat
```

#### Вариант 3: Ручной запуск

```bash
# Терминал 1: Backend
cd server
npm start

# Терминал 2: Frontend
npm run dev
```

## 📋 Проверка работоспособности

Запустите тестовый скрипт:

```bash
node scripts/test-crossplatform.js
```

Он проверит:
- ✅ Наличие всех необходимых файлов
- ✅ Корректность путей
- ✅ Настройки в package.json
- ✅ Конфигурацию Vite
- ✅ Наличие скриптов запуска

## 🔧 Настройка FIREBASE_PRIVATE_KEY

Важно: `FIREBASE_PRIVATE_KEY` должен быть в одну строку с экранированными переносами строк:

```env
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC...\n-----END PRIVATE KEY-----"
```

Скрипт `setup-env.js` автоматически проверяет формат этого ключа.

## 🌐 Порты и хосты

- **Frontend**: `http://127.0.0.1:5173`
- **Backend**: `http://localhost:3001`

Использование `127.0.0.1` вместо `localhost` обеспечивает более надежную работу на всех платформах.

## 📝 Структура проекта

```
VPN/
├── .env                    # Frontend переменные окружения
├── .env.example            # Пример frontend переменных
├── server/
│   ├── .env                # Backend переменные окружения
│   ├── .env.example        # Пример backend переменных
│   └── package.json        # Backend зависимости
├── scripts/
│   ├── start-all.js        # Универсальный скрипт запуска
│   └── test-crossplatform.js # Тест кроссплатформенности
├── setup-env.js            # Скрипт настройки .env
├── start-all.sh             # Скрипт запуска (Linux/Mac)
├── start-all.ps1            # Скрипт запуска (Windows PowerShell)
├── start-all.bat            # Скрипт запуска (Windows CMD)
└── package.json             # Frontend зависимости и скрипты
```

## ⚠️ Известные проблемы и решения

### Проблема: Vite не подхватывает переменные окружения

**Решение**: Убедитесь, что:
1. Файл `.env` использует переносы строк LF (не CRLF)
2. Переменные начинаются с `VITE_`
3. Запустите `npm run setup:env:fix` для автоматического исправления

### Проблема: Backend не может подключиться к Firebase

**Решение**: Проверьте:
1. `FIREBASE_PRIVATE_KEY` содержит `\n` для переносов строк
2. `FIREBASE_CLIENT_EMAIL` указан корректно
3. `FIREBASE_PROJECT_ID` совпадает с frontend

### Проблема: Порты заняты

**Решение**: Скрипты автоматически пытаются освободить порты, но если это не работает:
- **Linux/Mac**: `lsof -ti:3001 | xargs kill -9`
- **Windows**: `netstat -ano | findstr :3001` затем `taskkill /F /PID <PID>`

## 🧪 Тестирование на разных платформах

Проект протестирован и готов к работе на:
- ✅ macOS (darwin)
- ✅ Linux (Ubuntu, Debian, etc.)
- ✅ Windows 10/11 (PowerShell и CMD)

## 📚 Дополнительная документация

- `README.md` — основная документация проекта
- `SETUP_ENV.md` — подробная инструкция по настройке переменных окружения
- `WINDOWS_QUICK_START.md` — быстрый старт для Windows
- `UBUNTU_QUICK_START.md` — быстрый старт для Ubuntu/Linux

## 🆘 Поддержка

Если возникли проблемы:
1. Запустите `node scripts/test-crossplatform.js` для диагностики
2. Проверьте логи: `tail -f backend.log` (Linux/Mac) или `Get-Content backend.log -Tail 50 -Wait` (Windows)
3. Убедитесь, что все переменные окружения заполнены: `npm run setup:env`
