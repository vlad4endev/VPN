# Отчет: Реализация взаимодействия с 3x-ui API

## 📋 Обзор

Проект использует **централизованный сервисный класс `ThreeXUI`** для всех взаимодействий с панелью управления 3x-ui. Интеграция реализована через **прокси-сервер Vite** для обхода CORS ограничений и обеспечения безопасности.

---

## 🏗️ Архитектура

### 1. Основной сервис: `ThreeXUI` (`src/services/ThreeXUI.js`)

**Тип:** Singleton класс (экспортируется как единственный экземпляр)

**Назначение:** Централизованная точка доступа ко всем операциям с API 3x-ui

**Ключевые особенности:**
- Использует `axios` для HTTP-запросов
- Автоматическое управление сессией через cookies (`withCredentials: true`)
- Встроенное логирование всех запросов и ответов
- Санитизация чувствительных данных в логах
- Автоматическая проверка и восстановление авторизации

---

## 🔧 Конфигурация

### Переменные окружения

```env
# Credentials для авторизации в 3x-ui
VITE_XUI_USERNAME=your_username
VITE_XUI_PASSWORD=your_password
VITE_XUI_INBOUND_ID=your_inbound_id

# Настройки прокси (только для dev-режима)
XUI_HOST=http://your-server-ip:port/path
# Пример: XUI_HOST=https://84.201.161.204:40919/Gxckr4KcZGtB6aOZdw
```

### Прокси-конфигурация (`vite.config.js`)

**Маршрут:** `/api/xui/*` → проксируется на `XUI_HOST`

**Особенности:**
- Автоматическое добавление пути панели из `XUI_HOST`
- Нормализация путей (устранение двойных слэшей)
- Логирование всех прокси-запросов
- Поддержка HTTPS/HTTP

**Пример:**
```
Запрос: /api/xui/panel/api/inbounds
XUI_HOST: https://84.201.161.204:40919/Gxckr4KcZGtB6aOZdw
Результат: https://84.201.161.204:40919/Gxckr4KcZGtB6aOZdw/panel/api/inbounds
```

---

## 📡 Реализованные методы API

### 1. Авторизация (`login()`)

**Эндпоинт:** `POST /login`

**Формат запроса:**
```json
{
  "username": "string",
  "password": "string"
}
```

**Заголовки:**
- `Content-Type: application/json`
- `Accept: application/json`

**Особенности:**
- Автоматическое сохранение cookie сессии
- Проверка успешности по полю `success` в ответе
- Детальная обработка ошибок (404, 401, 403)

**Использование:**
- Вызывается автоматически через `ensureAuthenticated()`
- Используется при тестировании сессии серверов в админ-панели

---

### 2. Добавление клиента (`addClient()`)

**Эндпоинт:** `POST /panel/api/inbounds/addClient`

**Формат запроса (согласно реальному API 3x-ui):**
```json
{
  "id": 3,
  "settings": "{\"clients\": [{\"id\": \"uuid\", \"email\": \"email\", ...}]}"
}
```

**Структура клиента:**
```json
{
  "id": "uuid",
  "email": "user@example.com",
  "flow": "xtls-rprx-vision",
  "limitIp": 1,
  "totalGB": 0,
  "expiryTime": 0,
  "enable": true,
  "tgId": "",
  "subId": "",
  "reset": 0
}
```

**Особенности:**
- Проверка существования клиента перед добавлением
- Получение информации об инбаунде для валидации
- Fallback на получение списка инбаундов, если прямой запрос не работает
- **Важно:** `settings` передается как JSON-строка, а не объект

**Использование:**
- При получении ключа пользователем (`handleGetKey`)
- При обновлении подписки пользователя
- При создании нового пользователя админом

---

### 3. Удаление клиента (`deleteClient()`)

**Эндпоинт:** `POST /panel/api/inbounds/{inboundId}/delClient/{clientId}`

**Особенности:**
- Поиск клиента по email для получения UUID
- Использование UUID клиента в URL (не email)
- Fallback метод через обновление инбаунда (если эндпоинт не поддерживается)

**Использование:**
- При удалении пользователя админом
- При удалении собственного аккаунта пользователем

---

### 4. Обновление клиента (`updateClient()`)

**Эндпоинт:** `POST /panel/api/inbounds/{inboundId}/updateClient/{clientId}`

**Особенности:**
- Поиск клиента по email
- Сохранение обязательных полей (`id`, `email`)
- Fallback через обновление всего инбаунда

**Использование:**
- При обновлении лимитов трафика
- При изменении даты истечения подписки
- При изменении статуса клиента (enable/disable)

---

### 5. Получение статистики клиента (`getClientStats()`)

**Эндпоинт:** `GET /panel/api/clients/{clientId}/stats`

**Особенности:**
- Поиск клиента по email во всех инбаундах
- Нормализация времени истечения (секунды → миллисекунды)
- Возвращает полную информацию о трафике и лимитах

**Возвращаемые данные:**
```javascript
{
  email: string,
  uuid: string,
  inboundId: number,
  inboundTag: string,
  trafficUp: number,      // байты
  trafficDown: number,    // байты
  trafficTotal: number,   // байты
  expiryTime: number,     // timestamp в миллисекундах
  totalGB: number,        // лимит трафика в GB
  enable: boolean,
  limitIp: number
}
```

**Использование:**
- Отображение статистики в личном кабинете пользователя
- Проверка лимитов при использовании VPN

---

### 6. Получение инбаундов (`getInbounds()`, `getInbound()`)

**Эндпоинты:**
- `GET /panel/api/inbounds` - список всех инбаундов
- `GET /panel/api/inbounds/get/{id}` - конкретный инбаунд

**Особенности:**
- Нормализация ответа (`data.obj` или `data`)
- Обработка пустых ответов

**Использование:**
- Валидация инбаунда перед операциями
- Получение списка клиентов инбаунда

---

### 7. Поиск клиента (`getClientByEmail()`)

**Особенности:**
- Поиск по всем инбаундам
- Возвращает объект с данными клиента и инбаунда

**Использование:**
- Проверка существования клиента
- Получение полной информации о клиенте

---

## 🔐 Управление сессией

### Метод `ensureAuthenticated()`

**Назначение:** Автоматическая проверка и восстановление авторизации

**Логика работы:**
1. Выполняет тестовый запрос к `/panel/api/inbounds`
2. Если получен `401/403` или `success === false` → вызывает `login()`
3. Повторяет тестовый запрос для подтверждения
4. Обрабатывает ошибки 404 (проблемы с прокси/URL)

**Использование:**
- Вызывается автоматически перед каждым запросом к API
- Гарантирует валидную сессию для всех операций

---

## 📝 Логирование

### Перехватчики Axios

**Request Interceptor:**
- Логирует метод, URL, заголовки, параметры
- Санитизирует чувствительные данные (пароли, токены)
- Логирует тело запроса (с очисткой)

**Response Interceptor:**
- Логирует статус, данные ответа
- Ограничивает размер больших ответов в логах
- Детальное логирование ошибок (статус, данные, тип ошибки)

### Санитизация данных

**Очищаемые поля:**
- `password`, `token`, `apiKey`, `secret`, `key`, `auth`, `credentials`
- Заголовки: `authorization`, `cookie`, `x-api-key`

**Формат:** `***REDACTED***`

---

## 🎯 Использование в приложении

### 1. Получение VPN ключа (`handleGetKey`)

**Сценарий:** Пользователь запрашивает VPN ключ

**Процесс:**
1. Генерация UUID (`ThreeXUI.generateUUID()`)
2. Добавление клиента в 3x-ui (`ThreeXUI.addClient()`)
3. Обновление UUID в Firestore
4. Создание VLESS конфигурации (`ThreeXUI.createVLESSConfig()`)

**Обработка ошибок:**
- Проверка существования клиента
- Обработка сетевых ошибок
- Информативные сообщения пользователю

---

### 2. Удаление пользователя (`handleDeleteUser`)

**Сценарий:** Админ удаляет пользователя

**Процесс:**
1. Удаление клиента из 3x-ui (`ThreeXUI.deleteClient()`)
2. Удаление из Firestore
3. Обновление локального состояния

**Особенности:**
- Удаление из 3x-ui не блокирует удаление из Firestore
- Ошибки в 3x-ui логируются, но не прерывают процесс

---

### 3. Обновление подписки (`handleUpdateSubscription`)

**Сценарий:** Изменение тарифа или продление подписки

**Процесс:**
1. Обновление клиента в 3x-ui (`ThreeXUI.updateClient()`)
2. Обновление данных в Firestore
3. Обновление локального состояния

**Обновляемые поля:**
- `totalGB` - лимит трафика
- `expiryTime` - дата истечения
- `enable` - статус активности

---

### 4. Тестирование сессии сервера (`handleTestServerSession`)

**Сценарий:** Админ тестирует подключение к серверу 3x-ui

**Особенности:**
- Использует **специальный прокси** `/api/test-session` (не основной `/api/xui`)
- Прямой запрос к серверу через прокси Vite
- Детальная обработка CORS ошибок
- Информативные сообщения об ошибках

**Формат запроса:**
```json
{
  "serverIP": "84.201.161.204",
  "serverPort": 40919,
  "protocol": "https",
  "randompath": "/Gxckr4KcZGtB6aOZdw",
  "username": "user",
  "password": "pass"
}
```

**Прокси-обработчик:** `vite.config.js` → плагин `test-session-proxy`

---

## 🔄 Синхронизация данных

### Firestore ↔ 3x-ui

**Принцип:** Firestore - источник истины, 3x-ui - исполнитель

**Синхронизация:**
- При создании пользователя → создается в обоих системах
- При удалении → удаляется из обоих систем
- При обновлении → обновляется в обоих системах

**Обработка расхождений:**
- Ошибки в 3x-ui не блокируют операции в Firestore
- Логируются для последующего анализа
- Пользователю показываются информативные сообщения

---

## 🛡️ Обработка ошибок

### Типы ошибок

1. **Сетевые ошибки** (`ERR_NETWORK`, `ERR_FAILED`)
   - Детальное сообщение с диагностикой
   - Проверка CORS проблем
   - Рекомендации по решению

2. **HTTP ошибки** (404, 401, 403, 500)
   - Специфичные сообщения для каждого кода
   - Информация о возможных причинах

3. **Ошибки валидации**
   - Проверка существования инбаунда
   - Проверка существования клиента
   - Валидация обязательных полей

4. **Ошибки авторизации**
   - Автоматическое восстановление через `ensureAuthenticated()`
   - Детальные сообщения о проблемах с credentials

---

## 📊 Форматы данных

### Запросы

**Авторизация:**
```json
POST /login
{
  "username": "string",
  "password": "string"
}
```

**Добавление клиента:**
```json
POST /panel/api/inbounds/addClient
{
  "id": 3,
  "settings": "{\"clients\": [{...}]}"
}
```

**Удаление клиента:**
```
POST /panel/api/inbounds/{inboundId}/delClient/{clientId}
(без тела запроса)
```

### Ответы

**Успешный ответ:**
```json
{
  "success": true,
  "msg": "Операция выполнена успешно",
  "obj": {...}
}
```

**Ошибка:**
```json
{
  "success": false,
  "msg": "Описание ошибки",
  "obj": null
}
```

---

## 🔍 Особенности реализации

### 1. Нормализация ответов

Все методы используют единый подход:
```javascript
const payload = response.data
const value = payload?.obj || payload
```

Это позволяет работать с разными форматами ответов API.

### 2. Fallback методы

Для совместимости с разными версиями 3x-ui:
- `addClientFallback()` - через обновление инбаунда
- `deleteClientFallback()` - через обновление инбаунда
- `updateClientFallback()` - через обновление инбаунда

### 3. UUID генерация

Стандартный формат UUID v4:
```javascript
'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'
```

### 4. VLESS конфигурация

Формат URI:
```
vless://{uuid}@{server}:{port}?type=tcp&security=reality&sni={sni}&fp={fingerprint}&pbk={publicKey}&sid={shortId}#SkyPuth-VPN
```

---

## 🚀 Производительность

### Оптимизации

1. **Singleton паттерн** - один экземпляр сервиса на все приложение
2. **Автоматическая авторизация** - проверка только при необходимости
3. **Кеширование сессии** - через cookies браузера
4. **Ленивая загрузка** - данные загружаются только при необходимости

### Ограничения

- Все запросы синхронные (последовательные)
- Нет кеширования данных инбаундов
- Поиск клиента по email требует перебора всех инбаундов

---

## 🔒 Безопасность

### Реализованные меры

1. **Санитизация логов** - пароли и токены не логируются
2. **Прокси-сервер** - скрывает реальные credentials от клиента
3. **HTTPS поддержка** - для защищенных соединений
4. **Валидация данных** - проверка перед отправкой запросов

### Рекомендации

- Использовать HTTPS для всех соединений
- Регулярно обновлять credentials
- Мониторить логи на предмет подозрительной активности
- Ограничить доступ к админ-панели

---

## 📈 Метрики и мониторинг

### Логируемые события

- Все запросы к API (метод, URL, параметры)
- Все ответы (статус, данные)
- Все ошибки (тип, сообщение, контекст)
- Операции с клиентами (создание, удаление, обновление)

### Уровни логирования

- `debug` - детальная информация для разработки
- `info` - важные события и операции
- `warn` - предупреждения (некритичные ошибки)
- `error` - ошибки, требующие внимания

---

## 🐛 Известные проблемы и ограничения

### 1. CORS ограничения

**Проблема:** Прямые запросы из браузера к внешним серверам блокируются CORS

**Решение:** Использование прокси-сервера Vite (только в dev-режиме)

**Для продакшена:** Настроить прокси на уровне веб-сервера (nginx, Apache)

### 2. Различия в версиях API

**Проблема:** Разные версии 3x-ui могут иметь разные форматы ответов

**Решение:** Fallback методы и нормализация ответов

### 3. Поиск клиента по email

**Проблема:** Требует перебора всех инбаундов

**Решение:** Кеширование списка инбаундов (не реализовано, но возможно)

---

## 📚 Документация API 3x-ui

Проект использует следующие эндпоинты:

- `POST /login` - авторизация
- `GET /panel/api/inbounds` - список инбаундов
- `GET /panel/api/inbounds/get/{id}` - информация об инбаунде
- `POST /panel/api/inbounds/addClient` - добавление клиента
- `POST /panel/api/inbounds/{id}/delClient/{clientId}` - удаление клиента
- `POST /panel/api/inbounds/{id}/updateClient/{clientId}` - обновление клиента
- `GET /panel/api/clients/{id}/stats` - статистика клиента

---

## 🚀 Реализованные улучшения (2025-01-27)

### 1. Retry механизм с Exponential Backoff ✅

**Реализация:** Метод `retryRequest()` с настраиваемым количеством попыток и экспоненциальной задержкой

**Особенности:**
- Максимум 3 попытки по умолчанию
- Exponential backoff: задержка увеличивается как `delay * 2^attempt`
- Умная фильтрация: не повторяет для ошибок авторизации (401, 403) и валидации (400)
- Автоматическое логирование каждой попытки

**Использование:**
```javascript
await this.retryRequest(async () => {
  return await this.api.post('/endpoint', data)
}, 3, 1000) // 3 попытки, начальная задержка 1 сек
```

---

### 2. Кеширование клиентов ✅

**Реализация:** In-memory кеш с автоматическим обновлением и инвалидацией

**Особенности:**
- Кеш хранится в `Map<email, clientData>`
- Автоматическое обновление каждые 5 минут
- Инвалидация при изменениях (add/update/delete)
- Оптимизация поиска с O(1) вместо O(n*m)
- Fallback на прямой поиск, если кеш пуст

**Методы:**
- `refreshClientCache()` - обновление кеша
- `invalidateCache()` - инвалидация кеша
- Автоматическое использование в `getClientByEmail()` и `getClientStats()`

**Производительность:**
- Поиск клиента: **O(1)** вместо O(n*m)
- Снижение нагрузки на API при частых запросах

---

### 3. Circuit Breaker паттерн ✅

**Реализация:** Класс `CircuitBreaker` для защиты от каскадных сбоев

**Параметры:**
- Порог ошибок: 5
- Таймаут: 60 секунд
- Состояния: CLOSED → OPEN → HALF_OPEN

**Логика:**
- При 5+ ошибках подряд → переход в OPEN
- В OPEN состоянии все запросы блокируются на 60 секунд
- После таймаута → переход в HALF_OPEN (одна попытка)
- При успехе → возврат в CLOSED

**Использование:**
Все критические методы обернуты в `circuitBreaker.execute()`:
- `addClient()`
- `deleteClient()`
- Автоматическая защита от перегрузки API

---

### 4. Rate Limiting ✅

**Реализация:** Класс `RateLimiter` для ограничения частоты запросов

**Параметры:**
- Максимум: 10 запросов в секунду
- Окно: 1 секунда (скользящее)

**Особенности:**
- Автоматическое ожидание при превышении лимита
- Логирование задержек
- Защита от перегрузки API 3x-ui

**Использование:**
Все методы вызывают `await this.rateLimiter.throttle()` перед запросом

---

### 5. Health Check ✅

**Реализация:** Метод `healthCheck()` для мониторинга состояния API

**Проверяемые параметры:**
- `api_reachable` - доступность API
- `authenticated` - статус авторизации
- `inbound_accessible` - доступность инбаунда
- `response_time` - время отклика
- `circuit_breaker_state` - состояние circuit breaker
- `cache_status` - статус кеша

**Использование:**
```javascript
const health = await ThreeXUI.healthCheck()
console.log(health)
// {
//   api_reachable: true,
//   authenticated: true,
//   inbound_accessible: true,
//   response_time: 234,
//   circuit_breaker_state: { state: 'CLOSED', ... },
//   cache_status: { clients_cached: 15, cache_age: 120000 }
// }
```

---

### 6. Улучшенная обработка ошибок ✅

**Изменения:**
- Все методы обернуты в `circuitBreaker.execute()` и `retryRequest()`
- Автоматическая инвалидация кеша при изменениях
- Детальное логирование с контекстом

**Защита от ошибок:**
- Сетевые ошибки → retry с exponential backoff
- Множественные ошибки → circuit breaker открывается
- Частые запросы → rate limiter замедляет

---

## ✅ Выводы

### Сильные стороны

1. ✅ Централизованная архитектура (один сервис для всех операций)
2. ✅ Автоматическое управление сессией
3. ✅ Детальное логирование для отладки
4. ✅ Обработка ошибок с информативными сообщениями
5. ✅ Санитизация чувствительных данных
6. ✅ Fallback методы для совместимости
7. ✅ Прокси для обхода CORS
8. ✅ **Retry механизм с exponential backoff** (НОВОЕ)
9. ✅ **Кеширование клиентов для оптимизации** (НОВОЕ)
10. ✅ **Circuit Breaker для защиты от сбоев** (НОВОЕ)
11. ✅ **Rate Limiting для защиты API** (НОВОЕ)
12. ✅ **Health Check для мониторинга** (НОВОЕ)

### Области для дальнейшего улучшения

1. ⚠️ Batch операции для множественных клиентов
2. ⚠️ WebSocket для real-time обновлений статистики
3. ⚠️ Backend прокси для production (рекомендуется)
4. ⚠️ Secret Manager для credentials (рекомендуется)
5. ⚠️ Автоматическое определение версии API

---

**Дата создания отчета:** 2025-01-27  
**Дата последнего обновления:** 2025-01-27  
**Версия API 3x-ui:** Согласно реальным примерам из n8n  
**Статус:** ✅ Рабочая реализация с улучшениями

---

## 📝 Changelog

### 2025-01-27 - Критические улучшения (Финальная версия)

#### Исправления проблем:
- ✅ **Singleton паттерн** - защита от множественных инстансов и утечек памяти
- ✅ **Race condition в кеше** - добавлена блокировка для предотвращения дублирования запросов
- ✅ **Утечка памяти в RateLimiter** - добавлена периодическая очистка старых записей
- ✅ **Автоматическое определение версии API** - избавление от лишних fallback'ов

#### Новые компоненты:
- ✅ **TransactionManager** - транзакции с автоматическим rollback для синхронизации 3x-ui ↔ Firestore
- ✅ **SecretManager** - безопасное хранение credentials (поддержка AWS Secrets Manager, Azure Key Vault)
- ✅ **Production Proxy Server** - решение проблемы CORS в production

#### Ранее реализованные:
- ✅ Добавлен retry механизм с exponential backoff
- ✅ Реализовано кеширование клиентов (O(1) поиск)
- ✅ Добавлен Circuit Breaker паттерн
- ✅ Реализован Rate Limiting (10 req/sec)
- ✅ Добавлен health check метод
- ✅ Улучшена обработка ошибок во всех методах
- ✅ Автоматическая инвалидация кеша при изменениях

---

## 🆕 Новые компоненты (2025-01-27)

### 1. TransactionManager ✅

**Файл:** `src/services/TransactionManager.js`

**Назначение:** Обеспечивает атомарность операций между 3x-ui и Firestore с автоматическим rollback

**Методы:**
- `addClientTransaction()` - атомарное добавление клиента
- `deleteClientTransaction()` - атомарное удаление клиента
- `getFailedRollbacks()` - получение списка неудачных rollback'ов
- `markRollbackResolved()` - отметка rollback как обработанного

**Особенности:**
- Автоматический rollback при ошибках
- Логирование неудачных rollback'ов в Firestore (`failed_rollbacks`)
- Защита от расхождений данных между системами

**Использование:**
```javascript
import TransactionManager from './services/TransactionManager.js'

const transactionManager = new TransactionManager(ThreeXUI.getInstance(), db)

// Вместо прямого вызова
await transactionManager.addClientTransaction(
  email,
  { inboundId, uuid, options },
  { /* firestore data */ }
)
```

---

### 2. SecretManager ✅

**Файл:** `src/services/SecretManager.js`

**Назначение:** Безопасное хранение и загрузка credentials из разных источников

**Поддерживаемые источники:**
- `env` - переменные окружения (по умолчанию)
- `aws` - AWS Secrets Manager
- `azure` - Azure Key Vault
- `file` - локальный файл (.env.local)

**Использование:**
```javascript
import SecretManager from './services/SecretManager.js'

// Автоматическая инициализация (для браузера)
// Или вручную:
await SecretManager.initialize({ source: 'aws' })
const creds = SecretManager.get()
```

---

### 3. Production Proxy Server ✅

**Файл:** `server/proxy-server.js`

**Назначение:** Решение проблемы CORS в production окружении

**Особенности:**
- Проксирование всех запросов к 3x-ui API
- Проброс cookies для сессии
- Health check endpoint
- Специальный endpoint для тестирования сессии

**Запуск:**
```bash
cd server
npm install
npm start
```

**Docker:**
```bash
docker build -f Dockerfile.proxy -t xui-proxy .
docker run -p 3001:3001 --env-file server/.env xui-proxy
```

**Подробнее:** См. `PRODUCTION_SETUP.md`

---

## 🔧 Исправленные проблемы

### 1. Singleton паттерн ✅

**Проблема:** Возможные утечки памяти при множественных инстансах

**Решение:**
```javascript
class ThreeXUI {
  static instance = null
  
  constructor() {
    if (ThreeXUI.instance) {
      return ThreeXUI.instance
    }
    // ...
    ThreeXUI.instance = this
  }
  
  static getInstance() {
    if (!ThreeXUI.instance) {
      ThreeXUI.instance = new ThreeXUI()
    }
    return ThreeXUI.instance
  }
}
```

---

### 2. Race condition в кеше ✅

**Проблема:** Дублирование запросов при одновременном обновлении кеша

**Решение:**
```javascript
this.cacheRefreshing = false
this.cacheRefreshPromise = null

async refreshClientCache() {
  if (this.cacheRefreshing) {
    return this.cacheRefreshPromise // Ждем завершения
  }
  // ...
}
```

---

### 3. Утечка памяти в RateLimiter ✅

**Проблема:** Массив `requests` мог бесконечно расти

**Решение:**
```javascript
this.cleanupInterval = setInterval(() => {
  this.requests = this.requests.filter(
    time => Date.now() - time < this.windowMs
  )
}, this.windowMs * 2)

destroy() {
  clearInterval(this.cleanupInterval)
}
```

---

### 4. Автоматическое определение версии API ✅

**Проблема:** Слишком много fallback'ов указывало на несовместимость

**Решение:**
```javascript
async detectAPIVersion() {
  try {
    const response = await this.api.get('/server/status')
    this.apiVersion = response.data?.obj?.version
    this.useNewClientAPI = true
  } catch (error) {
    if (error.response?.status === 404) {
      this.apiVersion = 'legacy'
      this.useNewClientAPI = false
    }
  }
}
```

---

## 📚 Дополнительная документация

- **PRODUCTION_SETUP.md** - Полное руководство по настройке production окружения
- **3XUI_API_INTEGRATION_REPORT.md** - Этот отчет (детальная документация API)

