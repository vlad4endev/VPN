# 🔧 Настройка Pre-commit Hooks

Инструкция по настройке pre-commit hooks для автоматической проверки кода перед коммитом.

## Установка

### 1. Установите Husky

```bash
npm install --save-dev husky lint-staged
```

### 2. Инициализируйте Husky

```bash
npx husky install
```

### 3. Добавьте prepare script в package.json

```json
{
  "scripts": {
    "prepare": "husky install"
  }
}
```

### 4. Создайте pre-commit hook

```bash
npx husky add .husky/pre-commit "npx lint-staged"
```

Или скопируйте файл `.husky/pre-commit` из проекта.

## Что проверяется

### 1. Проверка на наличие секретов

- Проверяет, что `.env` не попадает в коммит
- Ищет потенциальные секреты в коде (пароли, API ключи, токены)
- Предупреждает о найденных секретах

### 2. Линтинг кода (если настроен)

- ESLint для JavaScript/JSX файлов
- Prettier для форматирования

## Настройка

### Отключение проверки для конкретного коммита

```bash
git commit --no-verify -m "your message"
```

⚠️ **Внимание**: Используйте только в крайних случаях!

### Исключение файлов из проверки

Отредактируйте `.lintstagedrc.js` для настройки проверок.

## Проверка работы

```bash
# Попробуйте закоммитить файл с секретом
echo "password = 'secret123'" > test.js
git add test.js
git commit -m "test"
# Должна появиться ошибка
```

## Устранение неполадок

### Hook не срабатывает

1. Проверьте, что Husky установлен: `ls -la .husky/`
2. Проверьте права на файл: `chmod +x .husky/pre-commit`
3. Переустановите Husky: `npx husky install`

### Ошибка "command not found"

Убедитесь, что все зависимости установлены:
```bash
npm install
```

---

**Готово!** Pre-commit hooks настроены и будут автоматически проверять код перед каждым коммитом.
