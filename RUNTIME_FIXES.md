# 🔧 Исправления runtime ошибок

## Исправленные проблемы

### 1. ❌ `setViewStore is not defined`
**Проблема:** Где-то в коде использовался `setViewStore`, который был удален при замене Zustand на локальные состояния.

**Решение:** 
- Уже исправлено ранее - `setViewStore` заменен на `setViewState`
- Проверено, что в коде больше нет использований `setViewStore`

### 2. ❌ `useNavigate() may be used only in the context of a <Router> component`
**Проблема:** В `LoginForm.jsx` использовался `useNavigate()` из `react-router-dom`, но Router не был настроен в приложении.

**Решение:**
- Удален импорт `useNavigate` из `react-router-dom`
- Удален вызов `const navigate = useNavigate()`
- Заменено использование `navigate('/')` на `onSetView('landing')`
- Теперь компонент использует только проп `onSetView` для навигации

### 3. ❌ `QueryClientProvider` без необходимости
**Проблема:** В `main.jsx` использовался `QueryClientProvider` из `@tanstack/react-query`, но все хуки React Query были удалены.

**Решение:**
- Удален импорт `QueryClientProvider` и `queryClient`
- Удалена обертка `<QueryClientProvider>` из рендера
- Приложение теперь рендерится напрямую без React Query

## Измененные файлы

1. **src/features/auth/components/LoginForm.jsx**
   - Удален импорт `useNavigate` из `react-router-dom`
   - Удален вызов `useNavigate()`
   - Заменено `navigate('/')` на `onSetView('landing')`

2. **src/app/main.jsx**
   - Удален импорт `QueryClientProvider` и `queryClient`
   - Упрощен рендер без обертки QueryClientProvider

## Результат

✅ Все runtime ошибки исправлены
✅ Приложение должно работать без ошибок
✅ Навигация работает через пропы вместо react-router
✅ Удалены неиспользуемые зависимости (react-router-dom, react-query)

## Примечание

Если в будущем понадобится добавить роутинг, нужно будет:
1. Установить `react-router-dom`
2. Настроить `<BrowserRouter>` в `main.jsx`
3. Использовать `<Routes>` и `<Route>` для навигации

