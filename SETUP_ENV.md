# 🔧 Инструкция по настройке переменных окружения

## ⚠️ Текущая проблема

В файле `.env` остались значения-заглушки. Нужно заменить их на реальные значения.

## 📝 Шаг 1: Откройте файл .env

```bash
# В терминале
nano .env

# Или в VS Code
code .env

# Или в любом другом редакторе
```

## 🔥 Шаг 2: Настройка Firebase

### Где получить значения:

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Выберите ваш проект (или создайте новый)
3. Нажмите на иконку ⚙️ (Settings) > **Project settings**
4. Прокрутите вниз до раздела **Your apps**
5. Если у вас еще нет веб-приложения, нажмите **Add app** > **Web** (иконка `</>`)
6. Скопируйте значения из конфигурации:

```javascript
const firebaseConfig = {
  apiKey: "AIza...",           // ← VITE_FIREBASE_API_KEY
  authDomain: "...",            // ← VITE_FIREBASE_AUTH_DOMAIN
  projectId: "...",              // ← VITE_FIREBASE_PROJECT_ID
  storageBucket: "...",          // ← VITE_FIREBASE_STORAGE_BUCKET
  messagingSenderId: "...",      // ← VITE_FIREBASE_MESSAGING_SENDER_ID
  appId: "1:..."                 // ← VITE_FIREBASE_APP_ID
};
```

### Заполните в .env:

```env
VITE_FIREBASE_API_KEY=AIzaSyC...  # Скопируйте apiKey
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012  # Число
VITE_FIREBASE_APP_ID=1:123456789012:web:abc123def456  # Полная строка
```

## 🖥️ Шаг 3: Настройка 3x-ui

### Где получить значения:

1. Откройте панель 3x-ui в браузере
2. Войдите с учетными данными администратора
3. Найдите ID инбаунда (Inbound ID), в который будут добавляться клиенты

### Заполните в .env:

```env
VITE_XUI_USERNAME=admin              # Ваш логин администратора 3x-ui
VITE_XUI_PASSWORD=your_password      # Ваш пароль администратора 3x-ui
VITE_XUI_INBOUND_ID=1                # ID инбаунда (обычно 1, 2, 3 и т.д.)
XUI_HOST=http://your-server-ip:2053  # Адрес панели 3x-ui
```

**Примеры XUI_HOST:**
- Если 3x-ui на том же компьютере: `XUI_HOST=http://localhost:2053`
- Если на другом сервере: `XUI_HOST=http://192.168.1.100:2053`
- Если с доменом: `XUI_HOST=https://xui.yourdomain.com`

## ✅ Шаг 4: Проверка

После заполнения всех переменных:

1. Сохраните файл `.env`
2. Перезапустите dev сервер (если он запущен):
   ```bash
   # Остановите сервер (Ctrl+C)
   # Запустите снова
   npm run dev
   ```

## 📋 Чек-лист заполнения

Убедитесь, что все эти переменные заполнены **реальными значениями** (не содержат `your_`, `here`, `example`):

- [ ] `VITE_FIREBASE_API_KEY` - начинается с `AIza`
- [ ] `VITE_FIREBASE_AUTH_DOMAIN` - содержит `.firebaseapp.com`
- [ ] `VITE_FIREBASE_PROJECT_ID` - ваш project ID
- [ ] `VITE_FIREBASE_STORAGE_BUCKET` - содержит `.appspot.com`
- [ ] `VITE_FIREBASE_MESSAGING_SENDER_ID` - число
- [ ] `VITE_FIREBASE_APP_ID` - начинается с `1:`
- [ ] `VITE_XUI_USERNAME` - ваш логин
- [ ] `VITE_XUI_PASSWORD` - ваш пароль
- [ ] `VITE_XUI_INBOUND_ID` - число (обычно 1)
- [ ] `XUI_HOST` - валидный URL

## 🆘 Если не знаете, где взять значения

### Firebase:
- Если у вас нет Firebase проекта: [Создайте новый проект](https://console.firebase.google.com/)
- Если проект есть, но не знаете, где конфигурация: Project Settings > Your apps > Web app

### 3x-ui:
- Если панель не установлена: нужно сначала установить 3x-ui на сервере
- Если панель установлена, но не знаете адрес: проверьте конфигурацию 3x-ui или спросите у администратора сервера

## ⚠️ Важно

- **НЕ коммитьте** файл `.env` в Git
- Храните пароли в безопасности
- Не делитесь файлом `.env` с другими

---

После заполнения всех переменных приложение должно запуститься без ошибок! 🚀

