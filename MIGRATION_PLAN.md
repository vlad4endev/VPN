# 🔄 План миграции секретов - Пошаговая инструкция

## ✅ Что уже сделано автоматически:

1. ✅ Удален `update_firebase_env.py` с хардкоженными секретами
2. ✅ Обновлен `.gitignore` для защиты секретов
3. ✅ Создан `.env.example` (шаблон)
4. ✅ Создан `server/.env.example` (шаблон для backend)
5. ✅ Создан `Dockerfile` (без секретов)
6. ✅ Создан `.dockerignore`
7. ✅ Создан `docker-compose.yml` (с Docker Secrets)
8. ✅ Создан скрипт проверки `scripts/migrate-secrets.js`
9. ✅ Создан GitHub Actions workflow

---

## 📋 Пошаговая миграция (30-60 минут)

### Шаг 1: Проверка текущего состояния (5 минут)

```bash
# 1. Проверьте, что .env в .gitignore
git check-ignore -v .env
# Должно вывести: .env:1:.env

# 2. Проверьте, что .env не в Git
git ls-files | grep .env
# Не должно быть вывода (кроме .env.example)

# 3. Запустите скрипт проверки
node scripts/migrate-secrets.js
```

**Если есть ошибки:** Следуйте инструкциям из вывода скрипта.

---

### Шаг 2: Создание резервной копии (2 минуты)

```bash
# Создайте резервную копию текущего .env
cp .env .env.backup

# Создайте резервную копию server/.env (если существует)
cp server/.env server/.env.backup 2>/dev/null || echo "server/.env не найден"
```

---

### Шаг 3: Обновление .env файлов (10 минут)

#### 3.1 Обновите root `.env`:

```bash
# Откройте .env в редакторе
nano .env
# или
code .env
```

**Важно:**
- ✅ Убедитесь, что секреты 3x-ui НЕ имеют префикс `VITE_`
- ✅ Измените `VITE_XUI_USERNAME` → `XUI_USERNAME`
- ✅ Измените `VITE_XUI_PASSWORD` → `XUI_PASSWORD`

**Пример правильного `.env`:**
```env
# Firebase (VITE_ - можно, это публичные ключи)
VITE_FIREBASE_API_KEY=AIza...
VITE_FIREBASE_AUTH_DOMAIN=skypathvpn.firebaseapp.com
# ...

# 3x-ui (БЕЗ VITE_ - это секреты!)
XUI_HOST=http://localhost:2053
XUI_USERNAME=admin
XUI_PASSWORD=your_real_password
XUI_INBOUND_ID=1

# Application
NODE_ENV=development
```

#### 3.2 Создайте `server/.env`:

```bash
# Создайте server/.env на основе .env.example
cp server/.env.example server/.env

# Отредактируйте с реальными значениями
nano server/.env
```

---

### Шаг 4: Обновление кода (5 минут)

**Проверьте, что код не использует `VITE_XUI_USERNAME` или `VITE_XUI_PASSWORD`:**

```bash
# Поиск использования VITE_XUI_USERNAME и VITE_XUI_PASSWORD
grep -r "VITE_XUI_USERNAME\|VITE_XUI_PASSWORD" src/ server/
```

**Если найдены:**
- Обновите код, чтобы использовать переменные без `VITE_` префикса
- Это уже должно быть сделано в предыдущих улучшениях безопасности

---

### Шаг 5: Проверка локальной работы (10 минут)

```bash
# 1. Перезапустите dev сервер
npm run dev

# 2. Проверьте в браузере:
# - Приложение загружается
# - Firebase работает
# - Backend proxy работает

# 3. Проверьте консоль браузера (F12)
# Не должно быть ошибок о отсутствующих переменных

# 4. Проверьте backend proxy
curl http://localhost:3001/health
# Должно вернуть: {"status":"ok",...}
```

---

### Шаг 6: Настройка GitHub Secrets (15 минут)

#### 6.1 Перейдите в GitHub репозиторий:
1. Settings → Secrets and variables → Actions
2. Нажмите "New repository secret"

#### 6.2 Добавьте следующие секреты:

**Firebase:**
```
FIREBASE_API_KEY = значение из .env
FIREBASE_AUTH_DOMAIN = значение из .env
FIREBASE_PROJECT_ID = значение из .env
FIREBASE_STORAGE_BUCKET = значение из .env
FIREBASE_MESSAGING_SENDER_ID = значение из .env
FIREBASE_APP_ID = значение из .env
```

**3x-ui (Backend):**
```
XUI_HOST = значение из .env (или server/.env)
XUI_USERNAME = значение из .env (или server/.env)
XUI_PASSWORD = значение из .env (или server/.env)
XUI_INBOUND_ID = значение из .env (или server/.env)
```

**Application:**
```
FRONTEND_URL = http://localhost:5173 (или ваш production URL)
ALLOWED_ORIGINS = http://localhost:5173,http://localhost:3000
```

**Опционально:**
```
RECAPTCHA_SITE_KEY = значение из .env (если используется)
```

---

### Шаг 7: Проверка Git (5 минут)

```bash
# 1. Проверьте статус
git status

# 2. Убедитесь, что .env файлы НЕ в staging
git diff --cached | grep ".env"

# 3. Проверьте .gitignore
git check-ignore -v .env
git check-ignore -v server/.env

# 4. Добавьте только безопасные файлы
git add .gitignore .env.example server/.env.example Dockerfile .dockerignore docker-compose.yml scripts/ .github/
git add SECURITY_SECRETS_MANAGEMENT.md MIGRATION_PLAN.md

# 5. Проверьте, что готово к коммиту
git status
```

---

### Шаг 8: Тестирование Docker (10 минут)

#### 8.1 Создайте .env.production (для Docker):

```bash
# Создайте .env.production на основе .env.example
cp .env.example .env.production

# Отредактируйте с production значениями
nano .env.production
```

#### 8.2 Соберите Docker образ:

```bash
# Соберите образ
docker build -t vpn-app:test .

# Проверьте размер образа (должен быть без .env файлов)
docker images | grep vpn-app
```

#### 8.3 Запустите контейнер:

```bash
# Запустите с .env.production
docker run --rm -p 3001:3001 \
  --env-file .env.production \
  vpn-app:test

# Или используйте docker-compose
docker-compose up
```

---

### Шаг 9: Финальная проверка (5 минут)

```bash
# 1. Запустите скрипт проверки
node scripts/migrate-secrets.js
# Должно вывести: ✅ Проверка безопасности пройдена!

# 2. Проверьте, что нет секретов в Git
git grep -i "password.*=" -- "*.js" "*.jsx" "*.py" | grep -v ".env.example" | grep -v "your_password"

# 3. Проверьте, что нет Firebase ключей в коде
git grep "AIza" -- "*.js" "*.jsx" "*.py" | grep -v ".env.example"

# 4. Проверьте .gitignore
cat .gitignore | grep -E "\.env|secrets"
```

---

### Шаг 10: Коммит изменений (2 минуты)

```bash
# 1. Создайте коммит
git commit -m "🔐 Безопасное управление секретами

- Удален update_firebase_env.py с хардкоженными секретами
- Добавлен .env.example и server/.env.example
- Обновлен .gitignore для защиты секретов
- Добавлен Dockerfile и .dockerignore
- Добавлена интеграция с GitHub Secrets
- Добавлен скрипт проверки безопасности

Все секреты теперь хранятся в переменных окружения"

# 2. НЕ ПУШИТЕ, пока не убедитесь, что всё работает!
# 3. Сначала проверьте локально
```

---

## ✅ Чек-лист после миграции

### Проверка безопасности:

- [ ] `.env` в `.gitignore`
- [ ] `.env.example` создан и не содержит секретов
- [ ] `server/.env.example` создан
- [ ] `update_firebase_env.py` удален
- [ ] Нет хардкоженных секретов в коде
- [ ] `scripts/migrate-secrets.js` проходит успешно
- [ ] `.env` файлы не в Git (`git ls-files | grep .env`)

### Проверка функциональности:

- [ ] Приложение работает локально (`npm run dev`)
- [ ] Backend proxy работает (`curl http://localhost:3001/health`)
- [ ] Firebase подключение работает
- [ ] Docker образ собирается без секретов
- [ ] Docker контейнер запускается с переменными окружения

### Проверка деплоя:

- [ ] GitHub Secrets настроены
- [ ] GitHub Actions workflow проходит
- [ ] Docker образ не содержит секретов
- [ ] Production .env файл создан на сервере (не в Git)

---

## 🚨 Что делать, если что-то сломалось

### Проблема: Приложение не запускается

```bash
# 1. Проверьте .env файл
cat .env

# 2. Проверьте переменные окружения
node -e "require('dotenv').config(); console.log(process.env)"

# 3. Восстановите из резервной копии
cp .env.backup .env
```

### Проблема: Backend proxy не работает

```bash
# 1. Проверьте server/.env
cat server/.env

# 2. Проверьте переменные окружения backend
cd server && node -e "require('dotenv').config(); console.log(process.env)"

# 3. Восстановите из резервной копии
cp server/.env.backup server/.env
```

### Проблема: Секреты попали в Git

```bash
# 1. Удалите из Git истории (КРИТИЧНО!)
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch .env" \
  --prune-empty --tag-name-filter cat -- --all

# 2. Force push (ОСТОРОЖНО!)
git push origin --force --all
```

**⚠️ ВАЖНО:** Если секреты попали в публичный репозиторий:
1. СРОЧНО смените все секреты (Firebase, 3x-ui и т.д.)
2. Используйте `git filter-branch` для очистки истории
3. Уведомите команду о компрометации

---

## 📚 Дополнительные ресурсы

- [SECURITY_SECRETS_MANAGEMENT.md](./SECURITY_SECRETS_MANAGEMENT.md) - Полное руководство
- [GitHub Secrets Documentation](https://docs.github.com/en/actions/security-guides/encrypted-secrets)
- [Docker Secrets Documentation](https://docs.docker.com/engine/swarm/secrets/)

---

**Дата:** 2025-01-27  
**Версия:** 1.0  
**Статус:** ✅ Готово к выполнению
