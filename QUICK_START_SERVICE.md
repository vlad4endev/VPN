# ⚡ Быстрый запуск сервиса

> Инструкция для быстрого запуска SkyPuth VPN

---

## 🚀 Способ 1: Использование скрипта (рекомендуется)

### Development режим:
```bash
cd /Users/vl4endev/Desktop/VPN
./start-service.sh dev
```

Откройте: **http://localhost:5173**

### Production режим:
```bash
cd /Users/vl4endev/Desktop/VPN
./start-service.sh prod
```

Откройте: **http://localhost:3001**

---

## 🚀 Способ 2: Ручной запуск

### Development (для разработки):
```bash
cd /Users/vl4endev/Desktop/VPN
npm run dev
```

### Production (для тестирования production сборки):
```bash
cd /Users/vl4endev/Desktop/VPN

# Сборка frontend
npm run build

# Запуск сервера
npm start
```

---

## ✅ Проверка

После запуска откройте в браузере:

- **Development**: http://localhost:5173
- **Production**: http://localhost:3001

---

## 🆘 Проблемы?

### Сервис не открывается:

1. **Остановите старые процессы**:
   ```bash
   lsof -ti:3001 | xargs kill -9
   lsof -ti:5173 | xargs kill -9
   ```

2. **Проверьте, что .env файл существует**:
   ```bash
   ls -la .env
   ```

3. **Установите зависимости**:
   ```bash
   npm install
   cd server && npm install && cd ..
   ```

4. **Используйте скрипт**:
   ```bash
   ./start-service.sh dev
   ```

---

## 📚 Подробная диагностика

См. `TROUBLESHOOTING.md` для детальной диагностики проблем.

---

**Готово!** Сервис должен запуститься. 🎉
