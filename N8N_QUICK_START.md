# Быстрый старт с n8n

## Что изменилось?

✅ **Фронтенд код НЕ требует изменений** - все API endpoints остаются теми же  
✅ **XUIService НЕ требует изменений** - продолжает работать как раньше  
✅ **Backend Proxy упрощен** - теперь только перенаправляет запросы в n8n

## Быстрая установка (5 минут)

### 1. Установите n8n

```bash
# Вариант 1: npm (глобально)
npm install -g n8n

# Вариант 2: Docker
docker run -d --name n8n -p 5678:5678 n8nio/n8n
```

### 2. Запустите n8n

```bash
n8n start
# Или если через Docker:
docker start n8n
```

Откройте в браузере: `http://localhost:5678`

### 3. Настройте переменные окружения

Создайте/обновите `server/.env`:

```env
N8N_BASE_URL=http://localhost:5678
PORT=3001
```

### 4. Запустите новый Backend Proxy

```bash
cd server
npm install
node n8n-webhook-proxy.js
```

### 5. Импортируйте workflows

1. Откройте n8n: `http://localhost:5678`
2. Нажмите **Workflows** → **Import from File**
3. Импортируйте файлы из папки `n8n-workflows/`:
   - `add-client.json`
   - `health-check.json`
4. **Активируйте** каждый workflow (переключатель в правом верхнем углу)

### 6. Проверьте работу

```bash
# Health Check
curl http://localhost:3001/api/vpn/health

# Должен вернуть:
# {
#   "status": "ok",
#   "service": "n8n-webhook-proxy",
#   "n8n": { "available": true }
# }
```

## Готово! 🎉

Теперь все запросы к `/api/vpn/*` будут обрабатываться через n8n workflows.

## Что дальше?

- См. `N8N_INTEGRATION.md` для детальной документации
- См. `N8N_MIGRATION_GUIDE.md` для полного руководства по миграции
- Создайте дополнительные workflows для других операций (delete-client, get-stats и т.д.)

## Откат на старую версию

Если нужно вернуться:

```bash
# В server/package.json:
"start": "node xui-backend-proxy.js"

# Или через PM2:
pm2 stop n8n-webhook-proxy
pm2 start server/xui-backend-proxy.js --name xui-backend-proxy
```

## Troubleshooting

**n8n не запускается:**
```bash
# Проверьте порт
lsof -i :5678

# Проверьте логи
n8n logs
```

**Webhook не срабатывает:**
- Убедитесь, что workflow **активирован**
- Проверьте URL в настройках Webhook node
- Проверьте логи n8n

**Ошибки в workflow:**
- Откройте workflow в n8n
- Запустите вручную с тестовыми данными
- Проверьте данные на каждом шаге
