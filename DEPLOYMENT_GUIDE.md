# 🚀 Руководство по развертыванию SkyPuth VPN

> Пошаговая инструкция по деплою проекта в production

---

## 📋 Содержание

1. [Подготовка к деплою](#1-подготовка-к-деплою)
2. [Локальная сборка](#2-локальная-сборка)
3. [Деплой на Vercel](#3-деплой-на-vercel)
4. [Деплой на собственный сервер](#4-деплой-на-собственный-сервер)
5. [Настройка Backend Proxy](#5-настройка-backend-proxy)
6. [Проверка работоспособности](#6-проверка-работоспособности)
7. [Мониторинг и поддержка](#7-мониторинг-и-поддержка)

---

## 1. Подготовка к деплою

### Шаг 1.1: Проверка переменных окружения

Убедитесь, что все переменные окружения заполнены:

```bash
# Проверка переменных
node check-env.js
```

### Шаг 1.2: Тестирование локально

```bash
# Сборка для production
npm run build

# Просмотр сборки
npm run preview
```

Проверьте, что все функции работают корректно.

### Шаг 1.3: Проверка безопасности

- ✅ Убедитесь, что `.env` не попал в Git
- ✅ Проверьте, что все секреты в переменных окружения
- ✅ Убедитесь, что `.gitignore` правильно настроен

---

## 2. Локальная сборка

```bash
# Установка зависимостей (если еще не установлены)
npm install

# Сборка проекта
npm run build
```

После сборки папка `dist/` будет содержать все статические файлы для деплоя.

---

## 3. Деплой на Vercel

### Шаг 3.1: Установка Vercel CLI

```bash
npm i -g vercel
```

### Шаг 3.2: Деплой

```bash
# В корне проекта
vercel
```

Следуйте инструкциям в терминале.

### Шаг 3.3: Настройка переменных окружения

1. Перейдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Settings** > **Environment Variables**
4. Добавьте все переменные из `.env.example`:
   - `VITE_FIREBASE_API_KEY`
   - `VITE_FIREBASE_AUTH_DOMAIN`
   - `VITE_FIREBASE_PROJECT_ID`
   - `VITE_FIREBASE_STORAGE_BUCKET`
   - `VITE_FIREBASE_MESSAGING_SENDER_ID`
   - `VITE_FIREBASE_APP_ID`
   - И другие необходимые переменные

### Шаг 3.4: Настройка домена

1. В настройках проекта перейдите в **Domains**
2. Добавьте ваш домен
3. Настройте DNS записи согласно инструкциям Vercel

---

## 4. Деплой на собственный сервер

### Шаг 4.1: Подготовка сервера

**Требования:**
- Ubuntu 20.04+ или Debian 11+
- Node.js 18+
- Nginx
- SSL сертификат (Let's Encrypt)

### Шаг 4.2: Установка зависимостей на сервере

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Node.js 18
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# Установка Nginx
sudo apt install -y nginx

# Установка Certbot для SSL
sudo apt install -y certbot python3-certbot-nginx
```

### Шаг 4.3: Копирование файлов

```bash
# На вашем локальном компьютере
npm run build
scp -r dist/* user@your-server:/var/www/skyputh-vpn/

# На сервере
sudo mkdir -p /var/www/skyputh-vpn
sudo chown -R $USER:$USER /var/www/skyputh-vpn
```

### Шаг 4.4: Настройка Nginx

Создайте файл `/etc/nginx/sites-available/skyputh-vpn`:

```nginx
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;
    
    # Редирект на HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name your-domain.com www.your-domain.com;
    
    # SSL сертификаты
    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    
    # SSL настройки
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    
    # Корневая директория
    root /var/www/skyputh-vpn;
    index index.html;
    
    # Проксирование к Backend Proxy
    location /api/vpn {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
    
    location /api/payment {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # Статические файлы
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Кэширование статических ресурсов
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # Gzip сжатие
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json;
}
```

### Шаг 4.5: Активация конфигурации

```bash
# Создание символической ссылки
sudo ln -s /etc/nginx/sites-available/skyputh-vpn /etc/nginx/sites-enabled/

# Проверка конфигурации
sudo nginx -t

# Перезагрузка Nginx
sudo systemctl reload nginx
```

### Шаг 4.6: Настройка SSL

```bash
# Получение SSL сертификата
sudo certbot --nginx -d your-domain.com -d www.your-domain.com

# Автоматическое обновление (настроено автоматически)
```

---

## 5. Настройка Backend Proxy

### Шаг 5.1: Установка зависимостей

```bash
cd server
npm install
```

### Шаг 5.2: Настройка переменных окружения

Создайте файл `server/.env`:

```env
# 3x-ui настройки
XUI_HOST=http://your-3xui-server:2053
XUI_USERNAME=admin
XUI_PASSWORD=your_password
XUI_INBOUND_ID=1

# Proxy настройки
PROXY_PORT=3001
PROXY_HOST=0.0.0.0
NODE_ENV=production

# CORS настройки
ALLOWED_ORIGINS=https://your-domain.com
FRONTEND_URL=https://your-domain.com
```

### Шаг 5.3: Запуск через PM2

```bash
# Установка PM2
npm install -g pm2

# Запуск proxy сервера
pm2 start server/proxy-server.js --name xui-proxy

# Сохранение конфигурации PM2
pm2 save

# Настройка автозапуска
pm2 startup
```

### Шаг 5.4: Проверка работы

```bash
# Проверка статуса
pm2 status

# Просмотр логов
pm2 logs xui-proxy

# Health check
curl http://localhost:3001/health
```

---

## 6. Проверка работоспособности

### Функциональные тесты

1. **Регистрация пользователя**
   - Откройте приложение
   - Зарегистрируйте нового пользователя
   - Проверьте создание в Firestore
   - Проверьте создание клиента в 3x-ui

2. **Вход пользователя**
   - Войдите с созданными учетными данными
   - Проверьте отображение личного кабинета

3. **Создание подписки**
   - Выберите тариф
   - Проверьте создание подписки
   - Проверьте получение конфигурации

4. **Админ-панель**
   - Войдите как администратор
   - Проверьте отображение списка пользователей
   - Проверьте функции управления

### Проверка консоли браузера

Откройте DevTools (F12) и проверьте:
- ✅ Нет критических ошибок в Console
- ✅ Нет ошибок 404/500 в Network
- ✅ Все запросы к Firebase и Backend Proxy успешны

### Проверка логов сервера

```bash
# Nginx логи
sudo tail -f /var/log/nginx/error.log
sudo tail -f /var/log/nginx/access.log

# PM2 логи
pm2 logs xui-proxy
```

---

## 7. Мониторинг и поддержка

### Настройка мониторинга

#### Uptime Robot

1. Зарегистрируйтесь на [UptimeRobot](https://uptimerobot.com/)
2. Добавьте мониторинг вашего домена
3. Настройте уведомления

#### Sentry (для отслеживания ошибок)

1. Создайте проект на [Sentry](https://sentry.io/)
2. Установите SDK:
   ```bash
   npm install @sentry/react
   ```
3. Настройте в коде (см. документацию Sentry)

### Резервное копирование

#### Firestore

```bash
# Экспорт данных Firestore
gcloud firestore export gs://your-bucket/backup-$(date +%Y%m%d)
```

#### Настройки сервера

```bash
# Резервное копирование конфигурации Nginx
sudo tar -czf nginx-backup-$(date +%Y%m%d).tar.gz /etc/nginx/

# Резервное копирование переменных окружения
sudo tar -czf env-backup-$(date +%Y%m%d).tar.gz /var/www/skyputh-vpn/.env
```

### Обновление приложения

```bash
# На сервере
cd /path/to/project
git pull origin main
npm install
npm run build
sudo cp -r dist/* /var/www/skyputh-vpn/

# Перезагрузка Nginx
sudo systemctl reload nginx

# Перезапуск Backend Proxy
pm2 restart xui-proxy
```

---

## 🔧 Устранение неполадок

### Проблема: 502 Bad Gateway

**Решение:**
1. Проверьте, запущен ли Backend Proxy: `pm2 status`
2. Проверьте логи: `pm2 logs xui-proxy`
3. Проверьте настройки прокси в Nginx

### Проблема: CORS ошибки

**Решение:**
1. Проверьте `ALLOWED_ORIGINS` в `server/.env`
2. Убедитесь, что домен добавлен в список разрешенных
3. Перезапустите Backend Proxy: `pm2 restart xui-proxy`

### Проблема: SSL сертификат не работает

**Решение:**
1. Проверьте конфигурацию Nginx: `sudo nginx -t`
2. Обновите сертификат: `sudo certbot renew`
3. Перезагрузите Nginx: `sudo systemctl reload nginx`

---

## 📝 Чек-лист деплоя

- [ ] Все переменные окружения заполнены
- [ ] Локальная сборка работает
- [ ] SSL сертификат настроен
- [ ] Nginx конфигурация проверена
- [ ] Backend Proxy запущен и работает
- [ ] Firestore правила безопасности настроены
- [ ] Мониторинг настроен
- [ ] Резервное копирование настроено
- [ ] Домен настроен и работает
- [ ] Все функции протестированы

---

**Готово! Ваше приложение развернуто и готово к работе.** 🎉
