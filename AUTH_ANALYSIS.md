# 🔐 Анализ системы аутентификации

## 📋 Обзор архитектуры

Система аутентификации использует **двухуровневый подход**:

1. **Firebase Anonymous Authentication** - для доступа к Firestore Database
2. **Custom Authentication System** - кастомная система пользователей с email/password через Firestore

---

## 🏗️ Архитектура аутентификации

### Уровень 1: Firebase Anonymous Auth

**Назначение:** Обеспечить доступ к Firestore Database для чтения/записи пользователей

**Реализация:**
- Автоматическая авторизация при загрузке приложения
- Используется `signInAnonymously(auth)` из Firebase SDK
- Обработка через `onAuthStateChanged` listener
- Состояние хранится в `firebaseUser`

**Код:**
```javascript
// Строки 790-853 VPNServiceApp.jsx
useEffect(() => {
  const unsubscribe = onAuthStateChanged(auth, async (user) => {
    if (!user) {
      await signInAnonymously(auth)
    }
  })
  return () => unsubscribe()
}, [])
```

**Требования:**
- В Firebase Console должна быть включена Anonymous Authentication
- Путь: Authentication > Sign-in method > Anonymous > Enable

---

### Уровень 2: Custom User Authentication

**Назначение:** Управление пользователями приложения (регистрация, вход, управление профилем)

**Хранилище данных:**
- Firestore Collection: `artifacts/${appId}/public/data/users_v4`
- `appId = 'skyputh'` (константа в коде)

**Структура пользователя:**
```javascript
{
  id: string,                    // Firestore document ID
  email: string,                 // Email пользователя (уникальный)
  passwordHash: string,          // Bcrypt hash пароля
  name: string,                  // Имя пользователя
  role: 'user' | 'admin',        // Роль пользователя
  plan: string,                  // Тарифный план
  uuid: string,                  // UUID для VPN ключа
  expiresAt: number | null,      // Дата истечения подписки (timestamp)
  tariffName: string,            // Название тарифа
  tariffId: string,              // ID тарифа
  phone: string,                 // Номер телефона (опционально)
  createdAt: string,             // Дата создания (ISO string)
  updatedAt: string              // Дата обновления (ISO string)
}
```

---

## 🔑 Основные методы аутентификации

### 1. Регистрация (`handleRegister`)

**Расположение:** Строки 1042-1248 VPNServiceApp.jsx

**Процесс:**
1. ✅ Проверка доступности Firestore Database
2. ✅ Проверка/выполнение Firebase Anonymous Auth (если необходимо)
3. ✅ Валидация входных данных:
   - Email (формат, длина до 255 символов)
   - Имя (минимум 2 символа, максимум 100)
   - Пароль (минимум 6 символов, для регистрации: буквы + цифры)
4. ✅ Проверка на существование пользователя с таким email
5. ✅ Хеширование пароля с помощью `bcrypt.hash(password, 10)`
6. ✅ Создание записи в Firestore через `addDoc()`
7. ✅ Обновление локального состояния (users, currentUser)
8. ✅ Установка `currentUser` и переход на dashboard

**Особенности:**
- Использует `bcryptjs` версии 3.0.3
- Salt rounds = 10
- При ошибке пытается удалить созданного пользователя из Firestore
- Таймаут операции: 10 секунд

**Валидация пароля при регистрации:**
- Минимум 6 символов
- Максимум 128 символов
- Должен содержать хотя бы одну букву
- Должен содержать хотя бы одну цифру

---

### 2. Вход (`handleLogin`)

**Расположение:** Строки 961-1039 VPNServiceApp.jsx

**Процесс:**
1. ✅ Валидация email и пароля
2. ✅ Поиск пользователя в локальном массиве `users` по email
3. ✅ Проверка пароля:
   - Если у пользователя есть `passwordHash` → `bcrypt.compare(password, user.passwordHash)`
   - Если нет `passwordHash` (старые записи) → проверка прямого сравнения `user.password === password`
4. ✅ Удаление `passwordHash` из объекта перед установкой в `currentUser`
5. ✅ Установка `currentUser` и переход на dashboard/admin

**Обратная совместимость:**
- Поддержка старых записей без `passwordHash`
- При обнаружении старой записи предлагает перерегистрацию

**Особенности:**
- Не требует Firebase Anonymous Auth (проверка только локальных данных)
- Пароль НЕ отправляется на сервер
- Все проверки выполняются в браузере

---

### 3. Выход (`handleLogout`)

**Расположение:** Строки 1251-1259 VPNServiceApp.jsx

**Процесс:**
1. ✅ Очистка `currentUser`
2. ✅ Закрытие модальных окон (`showKeyModal`)
3. ✅ Переход на landing page
4. ✅ Очистка сообщений об ошибках/успехе

**Особенности:**
- Firebase Anonymous Auth остается активной (не разрывает соединение)
- Сессия пользователя очищается только на клиенте

---

## 🛡️ Безопасность

### Хеширование паролей

**Библиотека:** `bcryptjs` версия 3.0.3

**Параметры:**
- Salt rounds: 10
- Алгоритм: bcrypt

**Особенности:**
- Пароли НИКОГДА не хранятся в открытом виде
- Хеш сохраняется в поле `passwordHash`
- Сравнение через `bcrypt.compare()` (защита от timing attacks)

### Валидация данных

**Email:**
- Регулярное выражение: `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`
- Максимальная длина: 255 символов
- Проверка на пустоту

**Пароль (вход):**
- Минимум 6 символов
- Максимум 128 символов

**Пароль (регистрация):**
- Минимум 6 символов
- Максимум 128 символов
- Хотя бы одна буква (A-Z, a-z)
- Хотя бы одна цифра (0-9)

**Имя:**
- Минимум 2 символа
- Максимум 100 символов
- Только буквы, пробелы, дефисы

---

## 📦 Зависимости

### Установленные пакеты:

```json
{
  "bcryptjs": "^3.0.3",      // Хеширование паролей
  "firebase": "^12.7.0"      // Firebase SDK (Auth + Firestore)
}
```

### Импорты в VPNServiceApp.jsx:

```javascript
import { getAuth, signInAnonymously, onAuthStateChanged } from 'firebase/auth'
import { getFirestore, collection, getDocs, addDoc, deleteDoc, doc, updateDoc } from 'firebase/firestore'
import bcrypt from 'bcryptjs'
```

---

## 🔄 Потоки данных

### Регистрация

```
User Form → Validation → bcrypt.hash() → Firestore (addDoc) → Local State (setUsers, setCurrentUser)
```

### Вход

```
User Form → Validation → Search in users[] → bcrypt.compare() → setCurrentUser → Navigate to dashboard
```

### Загрузка пользователей

```
Firebase Anonymous Auth → Firestore (getDocs) → setUsers → Available for login
```

---

## 📍 Критические точки кода

### 1. Инициализация Firebase Auth

**Файл:** VPNServiceApp.jsx, строки 88-99

```javascript
let app = null
let auth = null
let db = null

try {
  app = initializeApp(firebaseConfig)
  auth = getAuth(app)
  db = getFirestore(app)
} catch (error) {
  // Обработка ошибок
}
```

### 2. Анонимная авторизация

**Файл:** VPNServiceApp.jsx, строки 790-853

**Триггеры:**
- При монтировании компонента
- При изменении состояния `auth`

### 3. Загрузка пользователей

**Файл:** VPNServiceApp.jsx, строки 751-792

**Триггеры:**
- После успешной Firebase Anonymous Auth
- При изменении `firebaseUser`

---

## ⚠️ Известные проблемы и ограничения

### 1. Обратная совместимость со старыми записями

**Проблема:** Старые пользователи могут иметь пароли в открытом виде

**Текущая реализация:**
- Проверка наличия `passwordHash`
- Если отсутствует → сравнение прямого пароля
- Предложение перерегистрации

**Риск:** Старые пароли не безопасны

### 2. Локальное хранение passwordHash

**Проблема:** `passwordHash` хранится в массиве `users` в памяти

**Текущая реализация:**
- При установке `currentUser` → удаление `passwordHash`
- Но `users[]` содержит хеши

**Риск:** Хеши доступны в памяти (низкий риск, но не идеально)

### 3. Нет механизма сброса пароля

**Проблема:** Пользователь не может восстановить пароль

**Текущая реализация:** Отсутствует

### 4. Нет механизма смены пароля

**Проблема:** Пользователь не может изменить пароль

**Текущая реализация:** Отсутствует

### 5. Нет email верификации

**Проблема:** Email не проверяется при регистрации

**Текущая реализация:** Отсутствует

### 6. Нет ограничений на количество попыток входа

**Проблема:** Возможны brute-force атаки

**Текущая реализация:** Отсутствует

### 7. Зависимость от Firebase Anonymous Auth

**Проблема:** Если Anonymous Auth отключена → приложение не работает

**Текущая реализация:** Подробные сообщения об ошибках

---

## 🔧 Рекомендации для обновлений

### Приоритет 1: Критические улучшения безопасности

1. **Миграция старых паролей**
   - Скрипт для автоматической миграции пользователей без `passwordHash`
   - Принудительная смена пароля при следующем входе

2. **Удаление passwordHash из локального состояния**
   - Не хранить хеши в `users[]`
   - Проверка пароля только при входе, без сохранения хеша в памяти

3. **Защита от brute-force**
   - Ограничение попыток входа (например, 5 попыток за 15 минут)
   - Сохранение неудачных попыток в Firestore

### Приоритет 2: Функциональные улучшения

4. **Сброс пароля**
   - Механизм восстановления через email
   - Использование Firebase Auth для отправки email или собственный сервис

5. **Смена пароля**
   - Страница изменения пароля в профиле
   - Требование старого пароля для подтверждения

6. **Email верификация**
   - Отправка кода верификации при регистрации
   - Проверка email перед активацией аккаунта

7. **Двухфакторная аутентификация (2FA)**
   - Опциональная 2FA через TOTP
   - Использование библиотеки типа `otplib`

### Приоритет 3: Улучшения UX

8. **Сессии и токены**
   - JWT токены для управления сессиями
   - Автоматический выход при истечении сессии

9. **"Запомнить меня"**
   - Долгосрочные сессии через localStorage
   - Опция "Оставаться в системе"

10. **История входов**
    - Логирование успешных/неуспешных попыток входа
    - Отображение последней активности

---

## 📊 Текущая структура файлов

```
src/
├── VPNServiceApp.jsx          # Основной компонент (2864 строки)
│   ├── handleRegister()       # Регистрация (строки 1042-1248)
│   ├── handleLogin()          # Вход (строки 961-1039)
│   ├── handleLogout()         # Выход (строки 1251-1259)
│   ├── loadUsers()            # Загрузка пользователей (строки 751-792)
│   └── validateEmail/Password/Name()  # Функции валидации (строки 166-225)
│
├── services/
│   └── ThreeXUI.js            # Сервис для 3x-ui API (не связан с аутентификацией)
│
└── utils/
    ├── logger.js              # Система логирования
    └── envValidation.js       # Валидация переменных окружения
```

---

## 🧪 Тестирование

### Сценарии для тестирования

1. **Регистрация:**
   - ✅ Успешная регистрация с валидными данными
   - ✅ Регистрация с существующим email
   - ✅ Регистрация с невалидным email
   - ✅ Регистрация со слабым паролем
   - ✅ Регистрация при отсутствии Firebase Anonymous Auth

2. **Вход:**
   - ✅ Успешный вход с правильными данными
   - ✅ Вход с неверным паролем
   - ✅ Вход с несуществующим email
   - ✅ Вход со старым форматом пароля (без passwordHash)

3. **Выход:**
   - ✅ Корректный выход и очистка состояния
   - ✅ Проверка, что Firebase Anonymous Auth остается активной

---

## 🔗 Связанные файлы

- `FIREBASE_SETUP.md` - Инструкции по настройке Firebase
- `DEPLOYMENT.md` - Инструкции по развертыванию
- `package.json` - Зависимости проекта

---

## 📝 Примечания

- Все операции с паролями выполняются на клиенте (в браузере)
- Firebase используется только как база данных, не для аутентификации пользователей приложения
- Анонимная авторизация Firebase нужна только для доступа к Firestore
- Пользователи приложения не являются пользователями Firebase Auth

---

**Дата создания:** 2025-01-27  
**Версия документа:** 1.0  
**Статус:** Актуально

