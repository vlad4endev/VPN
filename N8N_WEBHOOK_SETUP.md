# 🔧 Настройка n8n Webhook для создания подписки

## Проблема: Webhook не зарегистрирован для POST запросов

**Ошибка:** `This webhook is not registered for POST requests. Did you mean to make a GET request?`

**Причина:** В n8n webhook настроен на метод **GET**, а приложение отправляет **POST** запрос. Для создания клиента VPN необходим метод **POST**, так как данные отправляются в теле запроса.

## Решение

### Шаг 1: Проверьте n8n

1. Откройте n8n: `https://n8n.skypath.fun` (или ваш URL)
2. Войдите в систему
3. Найдите workflow, который должен обрабатывать создание клиентов VPN

### Шаг 2: Настройте Webhook (ВАЖНО!)

1. В workflow должен быть узел **Webhook** (HTTP Request Trigger)
2. **КРИТИЧЕСКИ ВАЖНО:** Метод должен быть **POST** (не GET!)
   - Откройте настройки узла Webhook
   - В поле "НТТР - метод" (HTTP-method) выберите **POST**
   - Сейчас там стоит **GET** - это неправильно для создания клиента
3. Путь должен быть: `/webhook/8a8b74ff-eedf-4ad2-9783-a5123ac073ed`
   - Или используйте переменную окружения `N8N_WEBHOOK_ID` с этим ID

**Если метод GET:**
- ❌ Webhook не сможет принимать данные в теле запроса
- ❌ Приложение не сможет создать клиента
- ✅ Используется только для чтения данных (health check, получение списка)

**Если метод POST:**
- ✅ Webhook может принимать данные в теле запроса
- ✅ Приложение сможет создать клиента
- ✅ Правильный метод для создания ресурсов

### Шаг 3: Активируйте Workflow

**ВАЖНО:** Workflow должен быть **АКТИВЕН (Active)**!

1. В правом верхнем углу workflow нажмите кнопку **"Active"** или **"Activate"**
2. Workflow должен быть зеленым/активным
3. Только активные workflow могут получать webhook запросы

### Шаг 4: Проверьте Webhook URL

В настройках backend proxy (`server/n8n-webhook-proxy.js`) используется:

```javascript
N8N_BASE_URL = 'https://n8n.skypath.fun'
DEFAULT_WEBHOOK_ID = '8a8b74ff-eedf-4ad2-9783-a5123ac073ed'
```

Полный URL webhook: `https://n8n.skypath.fun/webhook/8a8b74ff-eedf-4ad2-9783-a5123ac073ed`

### Шаг 5: Проверьте переменные окружения

В файле `.env` (в корне проекта) убедитесь, что указаны правильные значения:

```env
N8N_BASE_URL=https://n8n.skypath.fun
N8N_WEBHOOK_ID=8a8b74ff-eedf-4ad2-9783-a5123ac073ed
N8N_WEBHOOK_ADD_CLIENT=https://n8n.skypath.fun/webhook/8a8b74ff-eedf-4ad2-9783-a5123ac073ed
```

### Шаг 6: Проверьте Webhook вручную

Вы можете протестировать webhook вручную:

```bash
curl -X POST https://n8n.skypath.fun/webhook/8a8b74ff-eedf-4ad2-9783-a5123ac073ed \
  -H "Content-Type: application/json" \
  -d '{"test": "data"}'
```

Если workflow активен, вы должны получить ответ (не 404/500).

### Шаг 7: Проверьте логи n8n

1. В n8n перейдите в раздел **Executions**
2. Посмотрите последние выполнения workflow
3. Если есть ошибки, они будут там

## Альтернативное решение

Если webhook ID изменился, обновите его в:

1. **Переменные окружения** (`.env`):
   ```env
   N8N_WEBHOOK_ID=новый-id-webhook
   ```

2. **Перезапустите backend proxy**:
   ```bash
   cd server
   npm start
   ```

## Что должен делать n8n workflow

Workflow должен:
1. Принять POST запрос на `/webhook/8a8b74ff-eedf-4ad2-9783-a5123ac073ed`
2. Получить данные:
   - `operation`: "add_client"
   - `category`: "new_subscription"
   - `clientId`: UUID пользователя
   - `inboundId`: ID инбаунда 3x-ui
   - `expiryTime`: Время окончания в миллисекундах
   - `limitIp`: Количество устройств
   - `serverIP`, `serverPort`: Данные сервера
   - `xuiUsername`, `xuiPassword`: Учетные данные для 3x-ui
   - `sessionCookie`: Сессия (может быть null)
   - И другие данные из запроса

3. Создать клиента в 3x-ui
4. Вернуть ответ:
   ```json
   {
     "success": true,
     "vpnUuid": "uuid-клиента",
     "sessionUpdated": false,
     "sessionCookie": "новая-сессия-если-обновлена"
   }
   ```

## Проверка через Backend Proxy Health

```bash
curl http://localhost:3001/api/vpn/health
```

Это должно показать статус подключения к n8n.
