/**
 * Скрипт для перегенерации уникальных subId для всех пользователей
 * 
 * ВАЖНО: Этот скрипт должен запускаться только администратором
 * и только когда необходимо обновить формат subId для всех пользователей
 * 
 * Использование:
 * - В консоли браузера на странице админ-панели:
 *   import { adminService } from './features/admin/services/adminService.js'
 *   adminService.regenerateAllSubIds().then(result => console.log(result))
 * 
 * Или через Node.js с Firebase Admin SDK (нужно настроить)
 */

console.log('⚠️  ВНИМАНИЕ: Этот скрипт перегенерирует subId для всех пользователей!')
console.log('Убедитесь, что вы понимаете последствия этого действия.')

// Скрипт нужно запускать вручную через консоль браузера или через отдельный Node.js скрипт с Firebase Admin SDK