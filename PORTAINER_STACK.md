# 🐳 Установка через Portainer

> Пошаговая инструкция по развертыванию SkyPuth VPN через Portainer Stack

---

## 📋 Предварительные требования

- **Portainer** установлен и работает
- **Docker** и **Docker Compose** установлены на сервере
- Доступ к Portainer UI

---

## 🚀 Способ 1: Импорт Stack в Portainer

### Шаг 1: Подготовка переменных окружения

Создайте файл `.env` с переменными окружения. Пример:

```env
# Режим работы
NODE_ENV=production
PROXY_PORT=3001

# Backend proxy настройки (3x-ui) - БЕЗ префикса VITE_!
XUI_HOST=http://your-3xui-server:2053
XUI_USERNAME=admin
XUI_PASSWORD=your_password_here
XUI_INBOUND_ID=1

# CORS настройки
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
FRONTEND_URL=https://yourdomain.com

# Firebase (для frontend - С префиксом VITE_!)
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abc123

# Опционально
VITE_RECAPTCHA_SITE_KEY=your_recaptcha_key
VITE_LOG_LEVEL=warn
```

### Шаг 2: Откройте Portainer

1. Войдите в Portainer UI
2. Перейдите в **Stacks** (в меню слева)
3. Нажмите **Add stack**

### Шаг 3: Настройка Stack

1. **Name**: `skyputh-vpn`
2. **Build method**: Выберите один из вариантов:

   #### Вариант A: Из Git Repository (рекомендуется)
   
   - **Repository URL**: `https://github.com/vlad4endev/VPN.git`
   - **Reference**: `main` (или нужная ветка)
   - **Compose path**: `docker-compose.portainer.yml`
   - Установите **Auto-update** если хотите автообновления

   #### Вариант B: Из файла
   
   - Выберите **Web editor**
   - Скопируйте содержимое `docker-compose.portainer.yml` в редактор

3. **Environment variables**: Добавьте все переменные из `.env` файла
   
   В Portainer можно добавить переменные:
   - Через **Environment variables** секцию
   - Или через **.env file** (если загружаете через Git)

4. Нажмите **Deploy the stack**

---

## 🚀 Способ 2: Через Git Repository в Portainer

### Шаг 1: Подготовка репозитория

Убедитесь, что в репозитории есть:
- ✅ `docker-compose.portainer.yml`
- ✅ `Dockerfile`
- ✅ `.dockerignore`

### Шаг 2: Создание Stack в Portainer

1. **Stacks** → **Add stack**
2. **Name**: `skyputh-vpn`
3. **Build method**: **Repository**
4. **Repository URL**: `https://github.com/vlad4endev/VPN.git`
5. **Reference**: `main`
6. **Compose path**: `docker-compose.portainer.yml`
7. **Environment variables**: Добавьте все переменные
8. **Deploy the stack**

---

## ⚙️ Настройка переменных окружения в Portainer

### Через UI:

1. Откройте ваш Stack в Portainer
2. Перейдите в **Editor** (или **Duplicate/Edit**)
3. Прокрутите вниз до **Environment variables**
4. Добавьте каждую переменную:
   - **Name**: `XUI_HOST`
   - **Value**: `http://your-server:2053`
   - Нажмите **Add**

### Через .env файл:

Portainer может загружать переменные из `.env` файла, если он есть в репозитории.

**⚠️ ВАЖНО**: НЕ коммитьте `.env` файл с реальными секретами в Git!

---

## 🔧 Рекомендуемые настройки в Portainer

### Resource Limits (опционально):

В редакторе Stack добавьте:

```yaml
deploy:
  resources:
    limits:
      cpus: '1.0'
      memory: 512M
    reservations:
      cpus: '0.5'
      memory: 256M
```

### Auto-update (опционально):

Включите **Auto-update** при создании Stack для автоматического обновления при push в Git.

---

## 📊 Мониторинг в Portainer

### Просмотр логов:

1. Откройте Stack → **Containers**
2. Выберите контейнер `skyputh-vpn`
3. Перейдите в **Logs**

### Проверка статуса:

1. **Stacks** → `skyputh-vpn`
2. Проверьте статус контейнера
3. Healthcheck статус отображается в UI

### Статистика ресурсов:

1. **Containers** → `skyputh-vpn`
2. Перейдите в **Stats** для просмотра использования CPU/RAM

---

## 🔄 Обновление Stack

### Через Portainer UI:

1. **Stacks** → `skyputh-vpn`
2. Нажмите **Editor** (или **Duplicate/Edit**)
3. Внесите изменения
4. Нажмите **Update the stack**

### Через Git (если включен Auto-update):

1. Внесите изменения в `docker-compose.portainer.yml`
2. Сделайте commit и push в Git
3. Portainer автоматически пересоберет и обновит Stack (если настроено)

---

## 🆘 Устранение неполадок

### Проблема: Stack не запускается

**Решение:**
1. Проверьте логи: **Containers** → `skyputh-vpn` → **Logs**
2. Проверьте переменные окружения в Stack
3. Убедитесь, что все обязательные переменные заполнены

### Проблема: Healthcheck не проходит

**Решение:**
```bash
# Выполните команду внутри контейнера
docker exec skyputh-vpn curl -f http://localhost:3001/health
```

### Проблема: Переменные окружения не применяются

**Решение:**
1. Убедитесь, что переменные добавлены в Stack
2. Пересоздайте Stack (удалите и создайте заново)
3. Проверьте синтаксис переменных (без пробелов вокруг `=`)

---

## 📝 Чек-лист установки

- [ ] Portainer установлен и работает
- [ ] Репозиторий доступен (или файлы загружены)
- [ ] Все переменные окружения подготовлены
- [ ] Stack создан в Portainer
- [ ] Контейнер запущен и работает
- [ ] Healthcheck проходит
- [ ] Приложение доступно по адресу

---

## 🎯 Быстрый старт (кратко)

1. Откройте Portainer → **Stacks** → **Add stack**
2. **Name**: `skyputh-vpn`
3. **Repository**: `https://github.com/vlad4endev/VPN.git`
4. **Compose path**: `docker-compose.portainer.yml`
5. Добавьте все переменные окружения
6. **Deploy the stack**

---

**Готово!** Ваш проект развернут через Portainer. 🚀
