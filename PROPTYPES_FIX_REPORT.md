# Исправление PropTypes для дат

## 🐛 Проблема

Получено предупреждение PropTypes:
```
Warning: Failed prop type: Invalid prop `user.createdAt` of type `string` supplied to `UserCard`, expected `number`.
```

## 🔍 Причина

Firestore может возвращать даты в разных форматах:
- Как `number` (timestamp в миллисекундах)
- Как `string` (ISO строка, например "2024-01-15T10:30:00.000Z")
- Как `Timestamp` объект (который сериализуется в строку)

PropTypes были настроены только на `number`, что вызывало предупреждения.

## ✅ Исправления

### 1. Обновлены PropTypes для UserCard

**Файл:** `src/features/admin/components/UserCard.propTypes.js`

**Изменения:**
- `createdAt`: теперь принимает `number` или `string`
- `expiresAt`: теперь принимает `number` или `string`

```javascript
createdAt: PropTypes.oneOfType([
  PropTypes.number,
  PropTypes.string, // Firestore может возвращать ISO строки
]),

expiresAt: PropTypes.oneOfType([
  PropTypes.number,
  PropTypes.string, // Firestore может возвращать ISO строки или Timestamp
]),
```

### 2. Обновлена валидация данных

**Файл:** `src/features/admin/utils/userValidation.js`

**Изменения:**
- Валидация `expiresAt` теперь принимает и строки, и числа
- Строки конвертируются в числа для проверки

```javascript
// Валидация даты истечения
if (user.expiresAt !== undefined && user.expiresAt !== null) {
  // Принимаем и number, и string (ISO строка)
  const expiresAtValue = typeof user.expiresAt === 'string' 
    ? new Date(user.expiresAt).getTime() 
    : user.expiresAt
  
  if (typeof expiresAtValue !== 'number' || isNaN(expiresAtValue) || expiresAtValue < 0) {
    errors.push('Дата истечения должна быть валидной временной меткой')
  }
}
```

### 3. Обновлена нормализация данных

**Файл:** `src/features/admin/utils/userValidation.js`

**Изменения:**
- `normalizeUser` теперь правильно конвертирует строковые даты в числа

```javascript
expiresAt: user.expiresAt != null 
  ? (typeof user.expiresAt === 'string' 
      ? new Date(user.expiresAt).getTime() 
      : Number(user.expiresAt)) 
  : null,
```

## ✅ Результат

- ✅ Предупреждения PropTypes устранены
- ✅ Компонент корректно работает с датами в любом формате
- ✅ Валидация правильно обрабатывает строковые даты
- ✅ Нормализация конвертирует строки в числа при сохранении

## 🔍 Проверка

После исправлений:
1. Перезагрузите приложение
2. Проверьте консоль браузера - предупреждения должны исчезнуть
3. Проверьте работу с датами:
   - Отображение дат работает корректно
   - Сохранение дат работает корректно
   - Валидация дат работает корректно

## 📝 Примечания

- `formatDate` уже умеет работать и со строками, и с числами (через `new Date()`)
- `new Date()` автоматически конвертирует ISO строки в Date объекты
- При сохранении все даты нормализуются в числа (timestamp)

---

*Дата исправления: $(date)*
*Статус: ✅ Исправлено*

