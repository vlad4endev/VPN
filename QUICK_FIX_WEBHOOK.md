# ⚡ БЫСТРОЕ ИСПРАВЛЕНИЕ: Изменить метод Webhook на POST

## Проблема
```
Error: This webhook is not registered for POST requests. Did you mean to make a GET request?
```

## Решение (2 шага)

### Шаг 1: Измените метод в n8n

1. Откройте n8n: `https://n8n.skypath.fun`
2. Найдите workflow с webhook ID: `8a8b74ff-eedf-4ad2-9783-a5123ac073ed`
3. Откройте узел **Webhook**
4. В разделе **"НТТР - метод"** (HTTP-method):
   - **Сейчас:** `GET` ❌
   - **Нужно:** `POST` ✅
5. Измените на **POST**
6. Сохраните workflow (Ctrl+S или Cmd+S)

### Шаг 2: Активируйте Workflow

1. Убедитесь, что workflow **АКТИВЕН** (зеленая кнопка "Active" в правом верхнем углу)
2. Если не активен - нажмите кнопку активации
3. Workflow должен быть зеленым/активным

## Готово! 🎉

Теперь webhook будет принимать POST запросы, и создание подписки должно работать.

## Проверка

После изменений, попробуйте снова создать подписку в личном кабинете. Ошибка должна исчезнуть.

## Если проблема осталась

1. Проверьте, что workflow действительно активен (зеленый статус)
2. Проверьте, что метод действительно изменен на POST
3. Перезапустите backend proxy (если нужно):
   ```bash
   cd server
   npm start
   ```
