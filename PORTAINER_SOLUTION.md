# 🔧 Решение проблем Portainer - Сводка

> Все решения проблем при развертывании через Portainer

---

## ⚠️ Проблема 1: "не удалось найти ссылку «main»"

**Решение**: 
- Используйте **Web Editor** метод (см. `PORTAINER_FIX.md`)
- Или убедитесь, что ветка `main` запушена: `git push -u origin main`

---

## ⚠️ Проблема 2: "Dockerfile: такого файла или каталога нет"

**Быстрое решение**:

1. **Закоммитьте и запушьте Dockerfile**:
   ```bash
   git add Dockerfile
   git commit -m "Update Dockerfile for Portainer"
   git push origin main
   ```

2. **Проверьте на GitHub**: 
   - Откройте `https://github.com/vlad4endev/VPN`
   - Убедитесь, что `Dockerfile` виден в корне

3. **Используйте Web Editor**:
   - Клонируйте проект на сервер: 
     ```bash
     cd /opt
     git clone https://github.com/vlad4endev/VPN.git skyputh-vpn
     ```
   - В Portainer используйте `portainer-stack-web-editor.yml` с путем `/opt/skyputh-vpn`

**Подробно**: см. `PORTAINER_DOCKERFILE_FIX.md`

---

## ✅ Рекомендуемое решение (Web Editor)

Если Repository метод не работает, используйте Web Editor:

### Шаг 1: Клонирование на сервер

```bash
# На сервере с Portainer
ssh your-server
cd /opt
git clone https://github.com/vlad4endev/VPN.git skyputh-vpn
cd skyputh-vpn
ls -la Dockerfile  # проверьте наличие
```

### Шаг 2: Настройка в Portainer

1. **Portainer** → **Stacks** → **Add stack**
2. **Name**: `skyputh-vpn`
3. **Build method**: **Web editor**
4. Скопируйте содержимое `portainer-stack-web-editor.yml`
5. Измените `context: /opt/skyputh-vpn` на ваш путь
6. Убедитесь, что `dockerfile: Dockerfile`
7. Добавьте все переменные окружения
8. **Deploy the stack**

---

## 📝 Правильный stack файл для Web Editor

```yaml
version: '3.8'

services:
  skyputh-vpn:
    build:
      context: /opt/skyputh-vpn  # ⚠️ путь где код на сервере
      dockerfile: Dockerfile      # ⚠️ Dockerfile должен быть там!
    container_name: skyputh-vpn
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      # ... все переменные окружения
    # ... остальное без изменений
```

---

## 🎯 Чек-лист перед развертыванием

- [ ] Dockerfile закоммичен и запушен на GitHub
- [ ] Dockerfile виден на GitHub в корне репозитория
- [ ] Если используете Web Editor, код склонирован на сервер
- [ ] В Portainer указан правильный путь в `context`
- [ ] В Portainer указан правильный путь в `dockerfile`
- [ ] Все переменные окружения добавлены

---

## 📚 Подробные инструкции

- **Проблема с веткой main**: `PORTAINER_FIX.md`
- **Проблема с Dockerfile**: `PORTAINER_DOCKERFILE_FIX.md`
- **Быстрое решение**: `PORTAINER_QUICK_FIX.md`
- **Полная инструкция**: `PORTAINER_INSTALL.md`

---

**Готово!** Следуйте инструкциям выше для решения проблем. 🎉
