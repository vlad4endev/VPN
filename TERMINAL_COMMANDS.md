# Команды для терминала

## ❌ НЕПРАВИЛЬНО (что вы пытались сделать):

```bash
# Это НЕ работает в терминале!
import TransactionManager from './services/TransactionManager.js'
```

**Почему:** `import` - это JavaScript синтаксис, который работает только внутри `.js` или `.jsx` файлов.

---

## ✅ ПРАВИЛЬНО - Что можно делать в терминале:

### 1. Запустить Proxy Server:

```bash
# Перейдите в директорию server
cd server

# Установите зависимости (если еще не установлены)
npm install

# Запустите proxy server
npm start
```

### 2. Проверить, что файлы существуют:

```bash
# Из корневой директории проекта
ls src/services/TransactionManager.js
ls src/services/ThreeXUI.js
```

### 3. Запустить frontend приложение:

```bash
# Из корневой директории проекта
npm run dev
```

### 4. Проверить proxy server (если запущен):

```bash
# В другом терминале
curl http://localhost:3001/health
```

---

## 📝 Где использовать `import`:

`import` используется **только внутри JavaScript файлов**, например:

### В файле `src/VPNServiceApp.jsx`:

```javascript
// В начале файла
import TransactionManager from './services/TransactionManager.js'
import ThreeXUI from './services/ThreeXUI.js'
import { db } from './firebase.js'

// Затем в коде используйте:
const transactionManager = new TransactionManager(
  ThreeXUI.getInstance(),
  db
)
```

---

## 🔍 Быстрая проверка через Node.js REPL:

Если вы хотите протестировать импорт в терминале, используйте Node.js REPL:

```bash
# Запустите Node.js REPL
node

# В REPL выполните:
> import('./src/services/TransactionManager.js').then(m => console.log(m.default))
```

Но это только для тестирования. В реальном коде используйте обычный `import`.

---

## 📚 Полезные команды:

```bash
# Проверить версию Node.js
node --version

# Проверить установленные пакеты
npm list

# Установить зависимости для proxy server
cd server && npm install

# Запустить proxy server в фоне
cd server && npm start &

# Остановить все Node.js процессы
pkill node
```

---

**Помните:** 
- Терминал (zsh/bash) - для команд системы и npm
- JavaScript файлы - для кода с `import`

