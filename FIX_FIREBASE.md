# 🔧 Исправление ошибки конфигурации Firebase

## ❌ Проблема
Ошибка: "Ошибка конфигурации Firebase. Убедитесь, что: 1. Анонимная авторизация включена в Firebase Console 2. Все переменные окружения заполнены правильно 3. Dev сервер перезапущен после изменения .env файла"

## ✅ Решение (пошагово)

### Шаг 1: Остановите dev сервер
1. Найдите терминал, где запущен `npm run dev`
2. Нажмите `Ctrl+C` (или `Cmd+C` на Mac)
3. Дождитесь полной остановки сервера

### Шаг 2: Проверьте .env файл
Убедитесь, что файл `.env` существует в корне проекта и содержит:
```
VITE_FIREBASE_API_KEY=AIzaSy...your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012
VITE_FIREBASE_APP_ID=1:123456789012:web:abc123def456
```

**⚠️ ВАЖНО:** Замените все значения на реальные из вашего Firebase проекта!

### Шаг 3: Запустите dev сервер ЗАНОВО
```bash
npm run dev
```

**ВАЖНО:** Vite загружает переменные окружения ТОЛЬКО при старте сервера!

### Шаг 4: Обновите страницу в браузере
- Нажмите `F5` или `Cmd+R` (Mac)
- Или закройте и откройте вкладку заново

### Шаг 5: Проверьте консоль браузера
1. Откройте DevTools (F12 или Cmd+Option+I)
2. Перейдите на вкладку "Console"
3. Найдите сообщения, начинающиеся с "🔍 Диагностика переменных окружения Firebase:"
4. Убедитесь, что все переменные показывают `✅` (а не `❌ НЕ ЗАГРУЖЕНО`)

## 🔥 Дополнительно: Включите анонимную авторизацию в Firebase

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект: **skypathvpn**
3. Перейдите: **Authentication** → **Sign-in method**
4. Найдите **Anonymous** в списке провайдеров
5. Нажмите на **Anonymous**
6. Включите переключатель **Enable**
7. Нажмите **Save**

## ⚠️ Важные замечания

- **Vite НЕ перезагружает переменные окружения автоматически!**
- После каждого изменения `.env` нужно **ОБЯЗАТЕЛЬНО** перезапускать dev сервер
- Простое обновление страницы в браузере НЕ поможет, если сервер не был перезапущен

## 🐛 Если проблема осталась

1. Проверьте консоль браузера - там будут детальные сообщения о том, какие переменные не загружены
2. Убедитесь, что в `.env` нет пробелов вокруг знака `=`
3. Убедитесь, что в `.env` нет кавычек вокруг значений (если они не нужны)
4. Попробуйте удалить `node_modules/.vite` и перезапустить сервер

