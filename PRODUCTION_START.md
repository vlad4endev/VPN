# 🚀 Запуск Production сервера - Инструкция

> Правильный способ запуска production версии приложения

---

## ⚡ Быстрый старт

### Вариант 1: Использование скрипта (рекомендуется)

```bash
cd /Users/vl4endev/Desktop/VPN
./start-service.sh prod
```

### Вариант 2: Ручной запуск

```bash
cd /Users/vl4endev/Desktop/VPN

# 1. Сборка frontend
npm run build

# 2. Установка backend зависимостей (если еще не установлены)
cd server && npm install && cd ..

# 3. Запуск production сервера
npm start
```

---

## ✅ После запуска

Откройте в браузере: **http://localhost:3001**

Должна загрузиться главная страница приложения.

---

## 🔍 Проверка

### 1. Health check:
```bash
curl http://localhost:3001/health
```

Ожидаемый ответ:
```json
{"status":"ok","service":"xui-proxy","timestamp":"...","uptime":...}
```

### 2. Frontend:
```bash
curl http://localhost:3001/ | head -5
```

Должен вернуть HTML (не "Cannot GET /").

### 3. В браузере:
Откройте: **http://localhost:3001**

---

## ⚙️ Переменные окружения (опционально)

В `.env` файле можно добавить:

```env
NODE_ENV=production
PROXY_PORT=3001
PROXY_HOST=0.0.0.0
```

**Примечание**: `NODE_ENV=production` теперь не обязателен - сервер автоматически раздает статические файлы, если папка `dist` существует.

---

## 🆘 Проблемы?

### Frontend не открывается:

1. **Убедитесь, что dist собран**:
   ```bash
   npm run build
   ls -la dist/
   ```

2. **Перезапустите сервер**:
   ```bash
   lsof -ti:3001 | xargs kill -9
   npm start
   ```

3. **Проверьте логи сервера**:
   Должна быть строка:
   ```
   📁 Serving static files from: /path/to/dist
   🌐 Frontend доступен на: http://localhost:3001
   ```

---

## 📚 Подробная диагностика

См. `PRODUCTION_FIX.md` для детального решения проблем.

---

**Готово!** Production сервер должен работать корректно. 🎉
