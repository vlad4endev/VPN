# 🚀 Основные команды для запуска проекта SkyPuth VPN

## 📋 Структура проекта

Проект состоит из следующих служб:
1. **Frontend приложение** (React + Vite) - порт `5173`
2. **Backend Proxy сервер** (Express) - порт `3001`
3. **Firebase** - облачная база данных (не требует запуска)
4. **3x-ui панель** - внешний VPN сервер (должен быть доступен)

---

## 🔧 Шаг 1: Первоначальная установка

### 1.1 Установка зависимостей основного проекта

```bash
# Перейти в корневую директорию проекта
cd /Users/vl4endev/Desktop/VPN

# Установить зависимости
npm install
```

### 1.2 Установка зависимостей Backend Proxy сервера

```bash
# Перейти в папку server
cd server

# Установить зависимости прокси-сервера
npm install

# Вернуться в корневую директорию
cd ..
```

---

## ⚙️ Шаг 2: Настройка переменных окружения

### 2.1 Создание .env файла в корне проекта

```bash
# В корневой директории проекта
touch .env
```

Заполните `.env` следующими переменными:

```env
# Firebase Configuration
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# 3x-ui API credentials
VITE_XUI_USERNAME=your_3xui_username
VITE_XUI_PASSWORD=your_3xui_password
VITE_XUI_INBOUND_ID=your_inbound_id

# VPN Server Configuration (опционально)
VITE_VLESS_SERVER=your-server-domain.com
VITE_VLESS_PORT=443

# 3x-ui Proxy settings (для dev режима)
XUI_HOST=http://your-server-ip:port
```

### 2.2 Создание .env файла для Backend Proxy (опционально)

```bash
# В папке server
touch server/.env
```

Заполните `server/.env`:

```env
# Backend Proxy Configuration
PROXY_PORT=3001
PROXY_HOST=0.0.0.0
NODE_ENV=development

# CORS Configuration (для production)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
FRONTEND_URL=http://localhost:5173
```

---

## 🚀 Шаг 3: Запуск проекта

### Вариант A: Режим разработки (Development)

Для полноценной работы проекта необходимо запустить **две службы одновременно**:

#### Терминал 1: Backend Proxy сервер

```bash
# Перейти в папку server
cd server

# Запустить Backend Proxy (с автоперезагрузкой)
npm run dev

# Или обычный запуск
npm start
```

Ожидаемый вывод:
```
🚀 XUI Backend Proxy запущен на 0.0.0.0:3001
🔒 Environment: development
🌐 Allowed origins: http://localhost:5173, http://localhost:3000
📊 Health check: http://0.0.0.0:3001/api/xui/health
🔍 Sessions debug: http://0.0.0.0:3001/api/xui/sessions
```

**Проверка работоспособности:**
```bash
curl http://localhost:3001/api/vpn/health
```

#### Терминал 2: Frontend приложение

```bash
# В корневой директории проекта
npm run dev
```

Ожидаемый вывод:
```
  VITE v7.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

**Приложение будет доступно по адресу:** `http://localhost:5173`

---

### Вариант B: Production режим

#### 1. Сборка Frontend приложения

```bash
# В корневой директории проекта
npm run build
```

Собранные файлы будут в папке `dist/`

#### 2. Предпросмотр Production сборки

```bash
npm run preview
```

Приложение будет доступно на `http://localhost:4173`

#### 3. Запуск Backend Proxy в Production

```bash
cd server

# Установить NODE_ENV
export NODE_ENV=production

# Запустить сервер
npm start
```

#### 4. Запуск с PM2 (рекомендуется для постоянной работы)

```bash
cd server

# Установить PM2 глобально (если еще не установлен)
npm install -g pm2

# Запустить Backend Proxy через PM2
npm run pm2:start

# Проверить статус
pm2 status

# Просмотр логов
pm2 logs xui-backend-proxy

# Остановить
npm run pm2:stop

# Перезапустить
npm run pm2:restart

# Сохранить конфигурацию PM2 для автозапуска
pm2 save
pm2 startup
```

---

## 📝 Полный скрипт запуска (Bash)

Создайте файл `start.sh` в корне проекта:

```bash
#!/bin/bash

# Цвета для вывода
GREEN='\033[0;32m'
BLUE='\033[0;34m'
YELLOW='\033[1;33m'
NC='\033[0m' # No Color

echo -e "${BLUE}🚀 Запуск SkyPuth VPN${NC}"
echo ""

# Проверка Node.js
if ! command -v node &> /dev/null; then
    echo -e "${YELLOW}❌ Node.js не установлен. Установите Node.js >= 18.0.0${NC}"
    exit 1
fi

NODE_VERSION=$(node -v | cut -d'v' -f2 | cut -d'.' -f1)
if [ "$NODE_VERSION" -lt 18 ]; then
    echo -e "${YELLOW}❌ Требуется Node.js >= 18.0.0. Текущая версия: $(node -v)${NC}"
    exit 1
fi

# Проверка .env файла
if [ ! -f .env ]; then
    echo -e "${YELLOW}⚠️  Файл .env не найден. Создайте его перед запуском.${NC}"
    exit 1
fi

# Проверка зависимостей
if [ ! -d "node_modules" ]; then
    echo -e "${GREEN}📦 Установка зависимостей основного проекта...${NC}"
    npm install
fi

if [ ! -d "server/node_modules" ]; then
    echo -e "${GREEN}📦 Установка зависимостей Backend Proxy...${NC}"
    cd server
    npm install
    cd ..
fi

# Запуск Backend Proxy в фоне
echo -e "${GREEN}🚀 Запуск Backend Proxy сервера...${NC}"
cd server
npm start &
BACKEND_PID=$!
cd ..

# Ожидание запуска Backend Proxy
sleep 3

# Проверка работоспособности Backend Proxy
if curl -s http://localhost:3001/api/vpn/health > /dev/null; then
    echo -e "${GREEN}✅ Backend Proxy запущен на http://localhost:3001${NC}"
else
    echo -e "${YELLOW}⚠️  Backend Proxy не отвечает. Проверьте логи.${NC}"
fi

# Запуск Frontend
echo -e "${GREEN}🚀 Запуск Frontend приложения...${NC}"
echo -e "${BLUE}💡 Frontend будет доступен на http://localhost:5173${NC}"
echo -e "${BLUE}💡 Для остановки нажмите Ctrl+C${NC}"
echo ""

npm run dev

# Очистка при завершении
trap "kill $BACKEND_PID 2>/dev/null" EXIT
```

Сделайте скрипт исполняемым и запустите:

```bash
chmod +x start.sh
./start.sh
```

---

## 🔍 Проверка работоспособности

### 1. Проверка Backend Proxy

```bash
# Health check
curl http://localhost:3001/api/vpn/health

# Проверка активных сессий
curl http://localhost:3001/api/xui/sessions
```

### 2. Проверка Frontend

1. Откройте `http://localhost:5173` в браузере
2. Проверьте консоль браузера (F12) на наличие ошибок
3. Проверьте Network tab - запросы к `/api/vpn/*` должны идти через прокси

### 3. Функциональные тесты

1. **Регистрация пользователя:**
   - Зарегистрируйте нового пользователя
   - Проверьте создание в Firestore
   - Проверьте создание клиента в 3x-ui

2. **Вход пользователя:**
   - Войдите с созданными учетными данными
   - Проверьте отображение личного кабинета

3. **Админ-панель:**
   - Войдите как администратор
   - Проверьте список пользователей

---

## 🛠️ Полезные команды

### Проверка версий

```bash
# Версия Node.js (требуется >= 18.0.0)
node --version

# Версия npm
npm --version

# Проверка установленных зависимостей
npm list --depth=0
cd server && npm list --depth=0
```

### Очистка и переустановка

```bash
# Удалить node_modules и package-lock.json
rm -rf node_modules package-lock.json
rm -rf server/node_modules server/package-lock.json

# Переустановить зависимости
npm install
cd server && npm install && cd ..
```

### Анализ бандла

```bash
npm run analyze
```

После выполнения откройте `dist/stats.html` для просмотра анализа.

### Проверка занятых портов

```bash
# Проверить, какие порты заняты
lsof -ti:5173  # Frontend (Vite)
lsof -ti:3001  # Backend Proxy
lsof -ti:4173  # Frontend Preview

# Освободить порт (если нужно)
kill -9 $(lsof -ti:5173)
kill -9 $(lsof -ti:3001)
```

---

## 🌐 Порты проекта

- **Frontend (Vite Dev Server)**: `http://localhost:5173`
- **Frontend (Vite Preview)**: `http://localhost:4173`
- **Backend Proxy**: `http://localhost:3001`
- **3x-ui панель**: настраивается в `.env` (например, `http://your-server:2053`)

---

## ⚠️ Важные замечания

1. **Node.js версия**: Требуется Node.js >= 18.0.0
2. **Переменные окружения**: Обязательно настройте `.env` файлы перед запуском
3. **Firebase**: Убедитесь, что Firebase проект настроен и правила Firestore применены
4. **3x-ui**: Backend Proxy **ОБЯЗАТЕЛЕН** для работы с 3x-ui API из браузера (обход CORS)
5. **Оба сервера должны работать одновременно** для полноценной работы проекта

---

## 🐛 Устранение проблем

### Проблема: "Cannot find module"

```bash
# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install
cd server && npm install && cd ..
```

### Проблема: "Port already in use"

```bash
# Найти процесс, использующий порт
lsof -ti:5173  # для Vite
lsof -ti:3001  # для Backend Proxy

# Убить процесс
kill -9 $(lsof -ti:5173)
kill -9 $(lsof -ti:3001)
```

### Проблема: "Firebase not initialized"

- Проверьте, что все переменные `VITE_FIREBASE_*` заполнены в `.env`
- Убедитесь, что файл `.env` находится в корне проекта
- Перезапустите dev сервер после изменения `.env`

### Проблема: "Backend Proxy не отвечает"

1. Убедитесь, что Backend Proxy запущен (`cd server && npm start`)
2. Проверьте, что порт 3001 свободен
3. Проверьте логи Backend Proxy на наличие ошибок
4. Проверьте переменные окружения в `server/.env`

### Проблема: "CORS ошибки"

- Убедитесь, что Backend Proxy запущен на порту 3001
- Проверьте, что запросы идут через `/api/vpn/*` (проксируется через Vite)
- Проверьте настройки CORS в `server/xui-backend-proxy.js`

---

## 📚 Дополнительная документация

- `README.md` - Основная документация проекта
- `STARTUP_COMMANDS.md` - Подробные команды запуска
- `START_BACKEND_PROXY.md` - Документация по Backend Proxy
- `SETUP_ENV.md` - Детальная настройка переменных окружения
- `DEPLOYMENT.md` - Руководство по развертыванию
- `QUICK_START.md` - Быстрый старт

---

## ✅ Чек-лист запуска

- [ ] Node.js >= 18.0.0 установлен
- [ ] Зависимости основного проекта установлены (`npm install`)
- [ ] Зависимости Backend Proxy установлены (`cd server && npm install`)
- [ ] Файл `.env` в корне проекта создан и заполнен
- [ ] Файл `server/.env` создан (опционально, если нужны кастомные настройки)
- [ ] Firebase проект настроен
- [ ] 3x-ui панель доступна
- [ ] Backend Proxy запущен (`cd server && npm start`)
- [ ] Frontend запущен (`npm run dev`)
- [ ] Приложение доступно на `http://localhost:5173`

---

**Готово! Проект должен быть полностью запущен и готов к работе.** 🚀
