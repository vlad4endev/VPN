# 🔑 Реализация уникального subId для 3x-ui

## Описание

Добавлена генерация уникального `subId` (Subscription ID) для каждого пользователя при регистрации. `subId` используется в 3x-ui для идентификации подписки клиента.

## Что было реализовано

### 1. Генерация subId

**Файл:** `src/features/vpn/services/ThreeXUI.js`

Добавлен метод `generateSubId()`:
- Формат: `timestamp (13 цифр) + случайное число (9 цифр)` = 22 символа
- Пример: `170412345678901234567`
- Гарантирует уникальность благодаря timestamp

### 2. Проверка уникальности

**Файл:** `src/features/auth/services/authService.js`

Создана функция `generateUniqueSubId()`:
- Проверяет уникальность `subId` в Firestore перед сохранением
- Делает до 10 попыток генерации нового `subId`, если найден дубликат
- Логирует все операции для отладки

### 3. Генерация при регистрации

**Файлы:**
- `src/features/auth/services/authService.js` - `createUserWithEmail()` и `signInWithGoogle()`
- `src/app/App.jsx` - `handleRegister()` и `handleGoogleSignIn()`

При регистрации нового пользователя:
1. Генерируется UUID для VPN подключения
2. Генерируется уникальный `subId` с проверкой в базе данных
3. Оба значения сохраняются в Firestore

### 4. Миграция существующих пользователей

Автоматическая миграция при входе:
- Если у существующего пользователя нет `subId`, он генерируется при следующем входе
- Миграция происходит в:
  - `loadUserData()` в `authService.js`
  - `handleEmailSignIn()` в `App.jsx`
  - `handleGoogleSignIn()` в `App.jsx`
  - `onAuthStateChanged` в `App.jsx`

### 5. Использование в 3x-ui

**Файл:** `src/features/dashboard/services/dashboardService.js`

`subId` передается в запросах к 3x-ui:
- При создании подписки (`createSubscription`)
- При получении ключа (`getKey`)
- В поле `subId` объекта клиента для 3x-ui

## Формат subId

```
Формат: [timestamp][random]
- timestamp: 13 цифр (Date.now() - миллисекунды)
- random: 9 цифр (0-999999999, дополняется нулями слева)
- Общая длина: 22 символа
- Тип: строка (числовая)

Пример: 170412345678901234567
```

## Проверка уникальности

Перед сохранением `subId` в Firestore:
1. Генерируется новый `subId`
2. Выполняется запрос к Firestore: `where('subId', '==', subId)`
3. Если найден дубликат - генерируется новый (до 10 попыток)
4. Если все попытки неудачны - добавляется дополнительная случайность

## Важно для Firestore

Для быстрого поиска рекомендуется создать индекс в Firestore:

1. Откройте Firebase Console
2. Firestore Database → Indexes
3. Создайте составной индекс:
   - Collection: `artifacts/skyputh/public/data/users_v4`
   - Fields:
     - `subId` (Ascending)
   - Query scope: Collection

Или добавьте правило в `firestore.indexes.json`:
```json
{
  "indexes": [
    {
      "collectionGroup": "users_v4",
      "queryScope": "COLLECTION",
      "fields": [
        {
          "fieldPath": "subId",
          "order": "ASCENDING"
        }
      ]
    }
  ]
}
```

## Использование в коде

### При создании клиента в 3x-ui:

```javascript
const operationData = {
  // ...
  subId: user.subId || '', // Уникальный subId для 3x-ui
  // ...
}
```

### Проверка наличия subId:

```javascript
if (!user.subId) {
  // subId отсутствует - это ошибка, должна быть миграция
  logger.warn('У пользователя отсутствует subId')
}
```

## Миграция существующих пользователей

Все существующие пользователи автоматически получат `subId` при следующем входе в систему. Никаких дополнительных действий не требуется.

## Отладка

Для проверки, что `subId` генерируется правильно:

1. Проверьте логи в консоли браузера:
   ```
   Auth: Уникальный subId сгенерирован для нового пользователя
   ```

2. Проверьте Firestore:
   - Откройте коллекцию `users_v4`
   - Проверьте поле `subId` у пользователя
   - Убедитесь, что значение уникально

3. Проверьте, что нет дубликатов:
   ```javascript
   // В Firebase Console → Firestore → Query
   // Попробуйте найти пользователя по subId
   where('subId', '==', 'ваш-subId')
   ```

## Примечания

- `subId` генерируется один раз при регистрации и остается постоянным для пользователя
- `subId` уникален в рамках всей базы данных (нет повторений)
- Если `subId` отсутствует у существующего пользователя, он будет автоматически сгенерирован при следующем входе
- `subId` передается в 3x-ui при создании/обновлении клиента
