# 🚀 System Monitoring - Quick Start

## ✅ Что создано

1. **Backend Routes** (`server/xui-backend-proxy.js`):
   - `GET /api/system/status` - Метрики системы
   - `GET /api/system/logs` - Логи системы

2. **Frontend Component** (`src/features/admin/components/SystemMonitor.jsx`):
   - 4 Info Cards (Status, CPU, RAM, Active Connections)
   - Line Chart для latency (Recharts)
   - Log Terminal с фильтрами

3. **Интеграция в AdminPanel**:
   - Новый таб "Мониторинг"

---

## 📦 Установка

### 1. Установить Recharts

```bash
npm install recharts
```

### 2. Настроить Proxy URL

В `.env`:
```env
VITE_PROXY_URL=http://localhost:3000
```

### 3. Запустить Backend Proxy

```bash
cd server
node xui-backend-proxy.js
```

---

## 🎯 Использование

1. Откройте Admin Panel
2. Перейдите на таб **"Мониторинг"**
3. Компонент автоматически обновляется каждые **5 секунд**

---

## 📊 Функционал

### Info Cards

- **Status**: Статус подключения к 3x-ui, время ответа
- **CPU**: Использование CPU с прогресс-баром (зеленый < 50%, желтый 50-80%, красный > 80%)
- **RAM**: Использование RAM с прогресс-баром
- **Active Connections**: Количество активных подключений

### Response Time Chart

График показывает последние **30 точек** времени ответа API в реальном времени.

### Log Terminal

- **Фильтры**: Все / Info / Warning / Error
- **Автопрокрутка**: Автоматически прокручивается к последним логам
- **Очистить**: Кнопка для очистки отображаемых логов

---

## ⚙️ Настройка

### Изменить интервал обновления

В `SystemMonitor.jsx`, строка ~70:
```javascript
}, 5000) // Измените на нужное значение (в миллисекундах)
```

### Изменить количество точек на графике

В `SystemMonitor.jsx`, строка ~20:
```javascript
const maxHistoryPoints = 30 // Измените на нужное значение
```

---

## 🔧 Backend Routes

### GET /api/system/status

Возвращает:
- CPU usage, load, cores
- RAM usage, used/total GB
- Uptime (seconds, formatted)
- 3x-ui connection status
- Active connections count

### GET /api/system/logs

Query params:
- `limit`: number (default: 100)
- `level`: 'all' | 'info' | 'warn' | 'error' (default: 'all')

**Примечание:** Сейчас возвращает пустой массив. Для полной интеграции требуется подключение XUILogger в backend.

---

## 🎨 Стилизация

Компонент использует ту же цветовую схему, что и остальные компоненты:
- `bg-slate-900` - фон карточек
- `border-slate-800` - границы
- `text-slate-200` - основной текст
- `text-slate-400` - вторичный текст

Цвета прогресс-баров:
- Зеленый: < 50%
- Желтый: 50-80%
- Красный: > 80%

---

## ✅ Готово!

После установки Recharts и запуска backend proxy, компонент будет работать автоматически.

**Дата:** 2025-01-27
