# ⚡ Быстрое решение: Dockerfile не найден

> Краткое руководство для решения проблемы с Dockerfile в Portainer

---

## 🚨 Проблема

**Ошибка**: `Не удалось развернуть стек: сбой при сборке с помощью compose: не удалось прочитать Dockerfile: такого файла или каталога нет`

---

## ✅ Быстрое решение (3 шага)

### Шаг 1: Убедитесь, что Dockerfile запушен на GitHub

```bash
cd /Users/vl4endev/Desktop/VPN

# Проверьте статус
git status Dockerfile

# Если есть изменения, закоммитьте
git add Dockerfile
git commit -m "Update Dockerfile for Portainer deployment"
git push origin main
```

### Шаг 2: Проверьте на GitHub

1. Откройте: `https://github.com/vlad4endev/VPN`
2. Убедитесь, что `Dockerfile` виден в корне репозитория
3. Убедитесь, что он в ветке `main`

### Шаг 3: Используйте Web Editor в Portainer

Если Repository метод все еще не работает, используйте Web Editor:

1. **Клонируйте проект на сервер**:
   ```bash
   ssh your-server
   cd /opt
   git clone https://github.com/vlad4endev/VPN.git skyputh-vpn
   cd skyputh-vpn
   ls -la Dockerfile  # проверьте наличие
   ```

2. **В Portainer**:
   - **Stacks** → **Add stack**
   - **Name**: `skyputh-vpn`
   - **Build method**: **Web editor**
   - Скопируйте содержимое `portainer-stack-web-editor.yml`
   - Измените `context: /opt/skyputh-vpn` на ваш путь
   - Убедитесь, что `dockerfile: Dockerfile`
   - Добавьте переменные окружения
   - **Deploy the stack**

---

## 📝 Правильный stack файл для Web Editor

```yaml
version: '3.8'

services:
  skyputh-vpn:
    build:
      context: /opt/skyputh-vpn  # ⚠️ путь где код на сервере
      dockerfile: Dockerfile      # ⚠️ Dockerfile должен быть там!
    container_name: skyputh-vpn
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      # ... все переменные окружения
    # ... остальное без изменений
```

---

## 🔍 Проверка перед развертыванием

```bash
# На сервере проверьте:
cd /opt/skyputh-vpn  # или ваш путь
ls -la Dockerfile    # должен существовать
head -5 Dockerfile   # должен быть валидным
```

---

## 📚 Подробная инструкция

См. `PORTAINER_DOCKERFILE_FIX.md` для полного руководства.

---

**Готово!** После этих шагов проблема должна быть решена. 🎉
