# 🚀 Руководство по оптимизации Node.js сервиса

## 📋 Содержание

1. [Анализ узких мест](#анализ-узких-мест)
2. [Что было оптимизировано](#что-было-оптимизировано)
3. [Пошаговая интеграция](#пошаговая-интеграция)
4. [Тестирование](#тестирование)
5. [Отладка](#отладка)
6. [Мониторинг производительности](#мониторинг-производительности)

---

## 🔍 Анализ узких мест

### Найденные проблемы:

1. **Синхронные операции файловой системы**
   - `fs.existsSync()` в `proxy-server.js` (2 места)
   - Блокирует event loop при проверке существования папки `dist`

2. **Отсутствие кэширования**
   - Каждый GET запрос к API выполняется заново
   - Сессии авторизации создаются при каждом запросе
   - Нет кэширования статических данных

3. **Однопоточность**
   - Node.js работает в одном потоке
   - Не используется многопоточность для обработки запросов

4. **Неоптимальная работа со статикой**
   - Можно улучшить заголовки кэширования

---

## ✅ Что было оптимизировано

### 1. Модуль кэширования (`cache.js`)
- ✅ In-memory кэш с поддержкой TTL
- ✅ Автоматическая очистка истекших записей
- ✅ Статистика использования кэша

### 2. Cluster Mode (`cluster.js`)
- ✅ Автоматический запуск в многопоточном режиме
- ✅ Использование всех доступных CPU ядер
- ✅ Автоматический перезапуск упавших workers
- ✅ Graceful shutdown

### 3. Оптимизированный Proxy Server (`proxy-server.optimized.js`)
- ✅ Убраны синхронные операции (`fs.existsSync` → `fs.promises.access`)
- ✅ Кэширование GET запросов к API (30 секунд)
- ✅ Кэширование сессий авторизации (1 час)
- ✅ Поддержка cluster mode
- ✅ Улучшенное кэширование статических файлов

### 4. Оптимизированный Payment Server (`payment-server.optimized.js`)
- ✅ Кэширование GET запросов (получение платежей)
- ✅ Инвалидация кэша при создании новых платежей
- ✅ Поддержка cluster mode

---

## 📝 Пошаговая интеграция

### ⚠️ ВАЖНО: Интеграция должна быть постепенной!

### Шаг 1: Резервное копирование

```bash
# Создайте резервную копию текущих файлов
cd /Users/vl4endev/Desktop/VPN/server
cp proxy-server.js proxy-server.js.backup
cp payment-server.js payment-server.js.backup
```

### Шаг 2: Установка новых модулей

Новые файлы уже созданы:
- ✅ `cache.js` - модуль кэширования
- ✅ `cluster.js` - cluster mode wrapper
- ✅ `proxy-server.optimized.js` - оптимизированный proxy server
- ✅ `payment-server.optimized.js` - оптимизированный payment server

**Проверьте, что все файлы на месте:**

```bash
ls -la server/cache.js server/cluster.js server/proxy-server.optimized.js server/payment-server.optimized.js
```

### Шаг 3: Тестирование новых модулей отдельно

#### 3.1. Тестирование модуля кэширования

Создайте тестовый файл `test-cache.js`:

```javascript
import { cache } from './cache.js'

// Тест 1: Сохранение и получение
cache.set('test_key', { data: 'test_value' }, 60)
const value = cache.get('test_key')
console.log('Test 1:', value?.data === 'test_value' ? '✅ PASS' : '❌ FAIL')

// Тест 2: Проверка TTL
cache.set('test_ttl', { data: 'expires' }, 1)
setTimeout(() => {
  const expired = cache.get('test_ttl')
  console.log('Test 2:', expired === null ? '✅ PASS' : '❌ FAIL')
  process.exit(0)
}, 2000)
```

Запустите тест:

```bash
node server/test-cache.js
```

**Ожидаемый результат:** Оба теста должны пройти (✅ PASS)

#### 3.2. Тестирование cluster mode

Создайте тестовый файл `test-cluster.js`:

```javascript
import { startCluster, getWorkerInfo } from './cluster.js'

startCluster(() => {
  const info = getWorkerInfo()
  console.log('Worker info:', info)
  console.log('✅ Cluster mode работает!')
  
  // Завершаем через 2 секунды
  setTimeout(() => {
    process.exit(0)
  }, 2000)
})
```

Запустите тест:

```bash
node server/test-cluster.js
```

**Ожидаемый результат:** Должны увидеть сообщения о запуске workers

### Шаг 4: Постепенная замена файлов

#### 4.1. Замена Proxy Server (РЕКОМЕНДУЕТСЯ НАЧАТЬ С ЭТОГО)

**Вариант A: Тестирование параллельно (БЕЗОПАСНЫЙ)**

1. Запустите оптимизированную версию на другом порту:

```bash
# В первом терминале - старый сервер
PROXY_PORT=3001 node server/proxy-server.js

# Во втором терминале - новый сервер
PROXY_PORT=3003 node server/proxy-server.optimized.js
```

2. Протестируйте новый сервер:

```bash
# Health check
curl http://localhost:3003/health

# Должен вернуть информацию о worker и кэше
```

3. Если все работает, замените файл:

```bash
cd server
mv proxy-server.js proxy-server.js.old
mv proxy-server.optimized.js proxy-server.js
```

**Вариант B: Прямая замена (БЫСТРЫЙ)**

```bash
cd server
mv proxy-server.js proxy-server.js.old
cp proxy-server.optimized.js proxy-server.js
```

#### 4.2. Замена Payment Server

Аналогично proxy server:

```bash
cd server
mv payment-server.js payment-server.js.old
cp payment-server.optimized.js payment-server.js
```

### Шаг 5: Обновление package.json (если нужно)

Если вы используете npm scripts, обновите их:

```json
{
  "scripts": {
    "start": "node server/proxy-server.js",
    "start:payment": "node server/payment-server.js"
  }
}
```

### Шаг 6: Перезапуск сервисов

```bash
# Если используете PM2
pm2 restart xui-proxy
pm2 restart payment-server

# Или если запускаете напрямую
node server/proxy-server.js
node server/payment-server.js
```

---

## 🧪 Тестирование

### Тест 1: Проверка работоспособности

```bash
# Health check
curl http://localhost:3001/health

# Должен вернуть:
# {
#   "status": "ok",
#   "service": "xui-proxy",
#   "worker": { "isWorker": true, "workerId": 1 },
#   "cache": { "total": 0, "valid": 0, "expired": 0 }
# }
```

### Тест 2: Проверка кэширования

```bash
# Первый запрос (должен быть медленнее)
time curl http://localhost:3001/api/xui/panel/api/inbounds

# Второй запрос (должен быть быстрее благодаря кэшу)
time curl http://localhost:3001/api/xui/panel/api/inbounds
```

**Ожидаемый результат:** Второй запрос должен быть быстрее, в логах должно быть `💾 Cache HIT`

### Тест 3: Проверка cluster mode

```bash
# Проверьте количество процессов
ps aux | grep "node.*proxy-server"

# Должно быть несколько процессов (master + workers)
# Количество workers = количество CPU ядер
```

### Тест 4: Нагрузочное тестирование

Установите `autocannon` для нагрузочного тестирования:

```bash
npm install -g autocannon
```

Запустите тест:

```bash
# Тест старой версии (если еще работает)
autocannon -c 10 -d 30 http://localhost:3001/health

# Тест новой версии
autocannon -c 10 -d 30 http://localhost:3001/health
```

**Ожидаемый результат:** Новая версия должна обрабатывать больше запросов в секунду

---

## 🐛 Отладка

### Включение детального логирования

Добавьте в начало файла:

```javascript
process.env.DEBUG = 'cache,cluster'
```

### Отладка через Node.js Inspector

```bash
# Запуск с inspector
node --inspect server/proxy-server.js

# Или с портом
node --inspect=9229 server/proxy-server.js
```

Затем откройте Chrome DevTools: `chrome://inspect`

### Логирование кэша

Добавьте endpoint для просмотра статистики кэша:

```javascript
app.get('/debug/cache', (req, res) => {
  res.json(cache.getStats())
})
```

Затем:

```bash
curl http://localhost:3001/debug/cache
```

### Отключение cluster mode для отладки

Установите переменную окружения:

```bash
ENABLE_CLUSTER=false node server/proxy-server.js
```

### Проверка синхронных операций

Используйте `clinic.js` для анализа:

```bash
npm install -g clinic
clinic doctor -- node server/proxy-server.js
```

---

## 📊 Мониторинг производительности

### Метрики для отслеживания:

1. **Время ответа API**
   - До оптимизации: ~200-500ms
   - После оптимизации: ~50-100ms (для кэшированных запросов)

2. **Использование CPU**
   - До оптимизации: ~25% (одно ядро)
   - После оптимизации: ~80-100% (все ядра)

3. **Пропускная способность (RPS)**
   - До оптимизации: ~100-200 req/s
   - После оптимизации: ~500-1000 req/s (зависит от количества ядер)

4. **Использование памяти**
   - Кэш занимает ~10-50MB (зависит от объема данных)

### Мониторинг через health endpoint

```bash
# Регулярная проверка
watch -n 1 'curl -s http://localhost:3001/health | jq'
```

### Мониторинг через PM2

```bash
pm2 monit
```

---

## 🔧 Настройка производительности

### Переменные окружения

```bash
# Отключить cluster mode (для отладки)
ENABLE_CLUSTER=false

# Задать количество workers вручную
CLUSTER_WORKERS=4

# Настройка портов
PROXY_PORT=3001
PAYMENT_SERVER_PORT=3002
```

### Настройка кэширования

В файлах `proxy-server.optimized.js` и `payment-server.optimized.js` можно изменить TTL:

```javascript
// Кэширование GET запросов (30 секунд)
cache.set(cacheKey, response, 30) // Измените на нужное значение

// Кэширование сессий (1 час = 3600 секунд)
cache.set(cacheKey, cookieValue, 3600) // Измените на нужное значение
```

---

## ⚠️ Важные замечания

### 1. Кэширование может привести к устаревшим данным

- GET запросы кэшируются на 30 секунд
- Если данные должны быть актуальными, уменьшите TTL или отключите кэширование для конкретных endpoints

### 2. Cluster mode использует больше памяти

- Каждый worker - это отдельный процесс Node.js
- На сервере с ограниченной памятью уменьшите количество workers

### 3. Сессии кэшируются между workers

- Кэш находится в памяти каждого worker процесса
- Сессии могут быть разными в разных workers (это нормально)

### 4. Откат изменений

Если что-то пошло не так:

```bash
cd server
mv proxy-server.js proxy-server.js.broken
mv proxy-server.js.old proxy-server.js
# Перезапустите сервер
```

---

## 📈 Ожидаемые результаты

После полной интеграции вы должны увидеть:

1. ✅ **Снижение времени ответа** на 50-80% для кэшированных запросов
2. ✅ **Увеличение пропускной способности** в 2-4 раза (зависит от количества CPU ядер)
3. ✅ **Снижение нагрузки на внешние API** благодаря кэшированию
4. ✅ **Более стабильная работа** под нагрузкой благодаря cluster mode

---

## 🆘 Решение проблем

### Проблема: Сервер не запускается

**Решение:**
1. Проверьте синтаксис: `node --check server/proxy-server.js`
2. Проверьте импорты: убедитесь, что `cache.js` и `cluster.js` существуют
3. Проверьте логи ошибок

### Проблема: Кэш не работает

**Решение:**
1. Проверьте, что модуль `cache.js` импортирован правильно
2. Добавьте логирование: `console.log('Cache stats:', cache.getStats())`
3. Убедитесь, что TTL не истек

### Проблема: Cluster mode не запускается

**Решение:**
1. Проверьте переменную окружения: `ENABLE_CLUSTER`
2. Проверьте количество CPU: `os.cpus().length`
3. Запустите без cluster mode для отладки: `ENABLE_CLUSTER=false node server/proxy-server.js`

### Проблема: Высокое использование памяти

**Решение:**
1. Уменьшите количество workers: `CLUSTER_WORKERS=2`
2. Уменьшите TTL кэша
3. Добавьте периодическую очистку кэша

---

## 📚 Дополнительные ресурсы

- [Node.js Cluster Module](https://nodejs.org/api/cluster.html)
- [Express.js Performance Best Practices](https://expressjs.com/en/advanced/best-practice-performance.html)
- [PM2 Documentation](https://pm2.keymetrics.io/docs/usage/quick-start/)

---

## ✅ Чеклист интеграции

- [ ] Резервное копирование создано
- [ ] Новые модули протестированы отдельно
- [ ] Proxy server заменен и протестирован
- [ ] Payment server заменен и протестирован
- [ ] Нагрузочное тестирование пройдено
- [ ] Мониторинг настроен
- [ ] Документация обновлена

---

**Удачи с оптимизацией! 🚀**
