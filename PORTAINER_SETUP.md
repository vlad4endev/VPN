# 🐳 Установка через Portainer - Полная инструкция

> Пошаговое руководство по развертыванию SkyPuth VPN через Portainer Stack

---

## 📋 Что вам нужно

- Portainer установлен и работает
- Доступ к Portainer UI (обычно `http://your-server:9000` или `https://your-domain:9000`)
- GitHub репозиторий доступен (или вы можете загрузить файлы вручную)

---

## 🚀 Способ 1: Из Git Repository (рекомендуется)

### Шаг 1: Откройте Portainer

1. Войдите в Portainer UI
2. Выберите ваш **Environment** (если у вас несколько)
3. Перейдите в **Stacks** (в меню слева)

### Шаг 2: Создание Stack

1. Нажмите **Add stack**
2. **Name**: `skyputh-vpn`
3. **Build method**: Выберите **Repository**
4. Заполните форму:

   ```
   Repository URL: https://github.com/vlad4endev/VPN.git
   Reference: main
   Compose path: portainer-stack.yml
   ```

5. **Auto-update**: Включите если хотите автоматические обновления при push в Git
6. Нажмите **Next**

### Шаг 3: Переменные окружения

В разделе **Environment variables** добавьте все переменные:

#### Обязательные переменные:

```
XUI_HOST=http://your-3xui-server:2053
XUI_USERNAME=admin
XUI_PASSWORD=your_password_here
XUI_INBOUND_ID=1

VITE_FIREBASE_API_KEY=AIza...
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abc123

ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
FRONTEND_URL=https://yourdomain.com
```

#### Опциональные:

```
VITE_LOG_LEVEL=warn
VITE_RECAPTCHA_SITE_KEY=your_key
```

### Шаг 4: Развертывание

1. Прокрутите вниз
2. Нажмите **Deploy the stack**
3. Дождитесь сборки и запуска (обычно 2-5 минут)

---

## 🚀 Способ 2: Web Editor (если нет доступа к Git)

### Шаг 1: Подготовка файла

1. Откройте файл `portainer-stack.yml` в проекте
2. Скопируйте его содержимое

### Шаг 2: Создание Stack в Portainer

1. **Stacks** → **Add stack**
2. **Name**: `skyputh-vpn`
3. **Build method**: Выберите **Web editor**
4. Вставьте содержимое `portainer-stack.yml` в редактор

### Шаг 3: Загрузка файлов (если нужна сборка из исходников)

Для сборки образа вам нужно загрузить файлы проекта на сервер:

```bash
# На сервере с Portainer
cd /path/to/portainer/data/compose/stacks/[stack-id]/

# Или создайте отдельную директорию
mkdir -p ~/skyputh-vpn
cd ~/skyputh-vpn

# Клонируйте репозиторий
git clone https://github.com/vlad4endev/VPN.git .

# Убедитесь, что Portainer может использовать этот путь
```

Затем в Portainer укажите путь к файлам в **Build context**.

### Шаг 4: Переменные окружения

Добавьте все переменные как в **Способе 1**.

### Шаг 5: Развертывание

Нажмите **Deploy the stack**

---

## 🚀 Способ 3: Из готового образа (если образ уже собран)

Если вы уже собрали образ и загрузили его в Docker registry:

1. **Stacks** → **Add stack**
2. **Name**: `skyputh-vpn`
3. **Build method**: **Web editor**
4. Используйте этот compose файл:

```yaml
version: '3.8'

services:
  skyputh-vpn:
    image: skyputh-vpn:latest  # или ваш registry/name:tag
    container_name: skyputh-vpn
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PROXY_PORT=3001
      # ... добавьте все переменные
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - vpn-network

networks:
  vpn-network:
    driver: bridge
```

---

## ✅ Проверка установки

### В Portainer UI:

1. **Stacks** → `skyputh-vpn`
2. Проверьте статус: должен быть **Active**
3. Перейдите в **Containers**
4. Проверьте контейнер `skyputh-vpn`:
   - **Status**: Running (зеленый)
   - **Health**: Healthy (зеленый)

### Просмотр логов:

1. **Containers** → `skyputh-vpn`
2. Нажмите **Logs**
3. Должны быть логи запуска без ошибок

### Проверка работоспособности:

```bash
# На сервере (или через Portainer → Containers → skyputh-vpn → Console)
curl http://localhost:3001/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "service": "xui-proxy",
  "timestamp": "...",
  "uptime": ...
}
```

---

## 🔧 Управление через Portainer

### Просмотр логов:

1. **Containers** → `skyputh-vpn` → **Logs**
2. Или **Stacks** → `skyputh-vpn` → **Logs**

### Перезапуск:

1. **Containers** → `skyputh-vpn` → **Recreate**
2. Или **Stacks** → `skyputh-vpn` → **Editor** → **Update the stack**

### Остановка:

1. **Stacks** → `skyputh-vpn` → **Stop**
2. Или **Containers** → `skyputh-vpn` → **Stop**

### Удаление:

1. **Stacks** → `skyputh-vpn` → **Remove**
2. Подтвердите удаление

---

## 🔄 Обновление Stack

### Автоматическое (если включен Auto-update):

1. Внесите изменения в Git репозиторий
2. Сделайте commit и push
3. Portainer автоматически обнаружит изменения и обновит Stack

### Ручное обновление:

1. **Stacks** → `skyputh-vpn` → **Editor**
2. Внесите изменения
3. Нажмите **Update the stack**

### Обновление из Git:

1. **Stacks** → `skyputh-vpn` → **Editor**
2. Если используется **Repository** method:
   - Измените **Reference** (если нужно)
   - Нажмите **Pull and redeploy**
3. Или нажмите **Pull and redeploy** в меню Stack

---

## ⚙️ Настройка переменных окружения в Portainer

### Добавление переменных:

1. **Stacks** → `skyputh-vpn` → **Editor**
2. Прокрутите вниз до **Environment variables**
3. Нажмите **Add variable**
4. Заполните:
   - **Name**: `XUI_HOST`
   - **Value**: `http://your-server:2053`
5. Повторите для всех переменных
6. Нажмите **Update the stack**

### Редактирование переменных:

1. **Stacks** → `skyputh-vpn` → **Editor**
2. Найдите переменную в **Environment variables**
3. Измените **Value**
4. Нажмите **Update the stack**

---

## 🎯 Рекомендуемые настройки в Portainer

### Resource Limits:

В редакторе Stack добавьте в секцию `skyputh-vpn`:

```yaml
deploy:
  resources:
    limits:
      cpus: '1.0'
      memory: 512M
    reservations:
      cpus: '0.5'
      memory: 256M
```

### Restart Policy:

Уже настроено: `restart: unless-stopped`

### Healthcheck:

Уже настроен в Stack

---

## 📊 Мониторинг в Portainer

### Просмотр статистики:

1. **Containers** → `skyputh-vpn` → **Stats**
2. Смотрите использование:
   - CPU
   - Memory
   - Network I/O
   - Block I/O

### Проверка событий:

1. **Containers** → `skyputh-vpn` → **Events**
2. Просмотрите историю событий контейнера

---

## 🆘 Устранение неполадок

### Проблема: Stack не запускается

**Решение:**
1. Проверьте логи: **Containers** → `skyputh-vpn` → **Logs**
2. Проверьте переменные окружения в Stack
3. Убедитесь, что все обязательные переменные заполнены

### Проблема: Build failed

**Решение:**
1. Проверьте доступность Git репозитория
2. Убедитесь, что `portainer-stack.yml` существует в репозитории
3. Проверьте путь в **Compose path** в настройках Stack

### Проблема: Healthcheck не проходит

**Решение:**
1. Откройте **Console** контейнера в Portainer
2. Выполните: `curl -f http://localhost:3001/health`
3. Проверьте логи на наличие ошибок

### Проблема: Переменные окружения не применяются

**Решение:**
1. Убедитесь, что переменные добавлены в Stack
2. Пересоздайте Stack (удалите и создайте заново)
3. Проверьте синтаксис переменных (без пробелов вокруг `=`)

---

## 📝 Чек-лист установки

- [ ] Portainer установлен и работает
- [ ] Доступ к Portainer UI
- [ ] Git репозиторий доступен (или файлы загружены)
- [ ] Stack создан в Portainer
- [ ] Все переменные окружения добавлены
- [ ] Stack развернут успешно
- [ ] Контейнер запущен (Status: Running)
- [ ] Healthcheck проходит (Health: Healthy)
- [ ] Приложение доступно по адресу

---

## 🎉 Готово!

После успешного развертывания ваше приложение будет доступно:

- **Локально**: `http://your-server-ip:3001`
- **Через домен**: `https://yourdomain.com` (если настроен Nginx/Proxy)

---

**Успешного развертывания!** 🚀
